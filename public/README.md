# Public folder

Site media (photos, videos, logos) is served from **Sanity CDN**, not from this folder.

To re-upload assets from a backup, run:

```bash
npm run seed:sanity-site-content
npm run seed:sanity-work-projects
```
