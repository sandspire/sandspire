# Ship Studio

> How professionals build with AI. No coding required.

## Brand Voice
See **`BRAND-VOICE.md`** for the full Sandspire tone-of-voice guide (read it before writing any copy). In short: **approachable but sharp** — like a smart friend who's great at their job. Plain-spoken, specific, quietly witty, never hypey. The **main homepage** (`/`) is the cinematic **social-first** concept (reels, photos & social, built on the locked brand strategy — formerly `/home-3`); the older web-design-led homepage now lives at `/home-1` and the second homepage at `/home-2`.

## Brand Identity
- Personality: Professional, human-first, and simple to use.
- Colors:
  - Primary: `--accent` (`#FF5E00`)
  - Secondary accent: `--accent-secondary` (`#F7941D`)
  - Neutrals: `--background` (`#FAF3E8`), `--foreground` (`#0D0D0D`), `--muted` (`#999999`)
  - Functional: `--success` (`#10B981`), `--warning` (`#F59E0B`), `--error` (`#EF4444`)
- Fonts: **two only** — **Geist** for all headlines/titles (wired through the `--font-display` CSS variable) and **Plus Jakarta Sans** for all body copy, buttons, nav, and captions. The old **Colitez Serif** and **Playfair Display** fonts have been fully removed. To change a heading font globally, edit `--font-display` in `app/globals.css`; both fonts load in `app/layout.tsx`.

## Pages
- **Homepage** (`/`) - **Cinematic, conversion-focused landing page** built on the locked social-first brand strategy (this is the former `/home-3`, promoted to the main homepage). A premium, video-first concept tied to the name **Sandspire**: a near-black, full-height hero that is an **interactive drag carousel of real client reels** with a hover-following **Instagram-story overlay** and the bottom-anchored headline **"If it's worth visiting, it's worth filming."** Sections in order: hero, a **kinetic statement** that reveals word-by-word with inline reel thumbnails, a pinned **video-scroll** reel section, a full-scroll **"What We Do"** services reveal (a 3D sphere + dashed ring **bloom** in from the centre of the words as you scroll, with three service cards sliding up at the corners), a dark brand marquee, a dark **process** strip (Plan · Shoot · Post · Report), a huge **"Make them hungry."** brand moment, and a final CTA ("Show us your brand — let's talk about what we'd shoot.") + FAQ, then a footer. **Copy is intentionally vertical-broad** (a UAE content studio for restaurants, gyms, salons, hotels, shops — food is one flagship vertical, not the whole identity). Self-contained (no Sanity fetch); uses the premium **`SandspireNavBar`** + **`SandspireFooter`** + Lenis smooth scroll (the smooth-scroll wrapper now lives inside `app/page.tsx` itself). Respects **reduced motion** and pauses the canvas when off-screen. Page content + copy live in **`app/page.tsx`** (constants `TRUST` / `PROCESS` at the top) plus the `components/sandspire/home3/*` pieces.
- **Home 1** (`/home-1`) - The **previous** main homepage, kept for reference/comparison after the social-first design took over `/`. The original **CMS-driven** layout: Hero video, logo marquee, who we are, services bento (compact 3-column grid on phones; full layout from tablet up), case studies, contact + FAQ, footer. Still pulls content from **Sanity**. Not linked from the public nav.
- **Services** (`/services`) - **Awwwards-style services landing page**, designed as a sibling to `/home-3` (same cinematic cream/near-black rhythm, Geist + Plus Jakarta, single `#FF5E00` accent). Sections in order: a **WebGL "ember field" hero** (a bespoke `ogl` fragment shader of brand-orange embers smouldering on near-black that tracks the cursor) with the headline **"Everything your brand needs to get seen."** and a drifting service marquee; a **kinetic word-reveal thesis** ("We shoot the content that gets you found, build the site that earns the booking, and wire up AI for the busywork."); **"The content studio"** — an interactive **cursor-tracking video-reveal index** of the five content services (hovering a row floats that service's real client reel toward the cursor; on touch it's a stacked thumbnail list); **"The build & automation studio"** — a cream grid of the four secondary services (web, brand, SEO, AI, numbered 06–09); a **horizontally-pinned process scroll** (Brief → Plan → Shoot → Publish → Report); a **"Ways to work"** band (one-off shoot / monthly content / full management, no prices); a big brand moment **"Get seen. Get visited."**; and a final **CTA + services FAQ + footer**. Self-contained (no Sanity fetch); uses the global `SandspireNavBar` + `SandspireFooter` + Lenis smooth scroll; respects reduced motion and pauses the WebGL canvas off-screen. Built on the existing `motion/react` + `lenis` + `ogl` stack — **no GSAP/Three.js** were added, to stay within the Cloudflare Worker size limits (same reason `/home-3` avoids them). The top-nav **"Services"** link now points here (was `/#services`).
- **Home 2** (`/home-2`) - Alternate homepage from Figma **[Things — node 349:142](https://www.figma.com/design/DAnc9vXaZN9VHQcCosnCm3/Things?node-id=349-142)** (MacBook Air 14): full-bleed **hero image** (`HeroImage.png`) with integrated logo rail, **Our Work** spotlight (project copy pinned on the left + **stacking scroll videos** for 3 Fils, eatkanji, and slrp.ramen on the right, with orange stripe backdrop), **Service Suite** bento grid (serif section title; Jakarta on cards), **Our 360° Showreel**, contact + FAQ, **sticky footer** with bright orange stripe glow. Smooth scrolling on this page only.
- **About** (`/about`) - **Cinematic, awwwards-style studio page**, rebuilt as a sibling to `/home-3` + `/services` + `/contact` (replaces the old Sanity-driven two-column page). Self-contained (no Sanity fetch), Lenis smooth scroll, shared nav over the dark hero, single `#FF5E00` accent. Sections in order: a **WebGL "sand field" hero** (a featherweight `ogl` ember/grain shader of drifting warm sand on near-black, with a cursor-following glow — the name *Sandspire* made literal) under the bottom-anchored headline **"The studio behind the feed."**; a **kinetic manifesto** that reveals word-by-word with an orange payoff ("The best place in the neighbourhood shouldn't lose to the one with the better feed. So we make sure it doesn't."); a dark **count-up stats** strip (the operating model in honest numbers: 5 brands · 1 shoot day · 9 crafts · 100% you sign off); an interactive **"What we believe"** principles list (five rules, hover-warm rows); a dark **showcase** of live client reels + a scrolling capabilities marquee; the giant **"From sand, a spire."** brand moment over the live shader; then a final CTA ("Let's make your feed worth the visit.") + footer matched to `/home-3`. Built on the existing `motion/react` + `lenis` + `ogl` stack — **no GSAP/Three.js added** (same Cloudflare-Worker-size reason as the other pages). Respects **reduced motion** (shaders paint one still frame; animations resolve instantly) and is kind to GPUs (the hero shader pauses off-screen; the second shader **lazy-mounts** only when its section nears the viewport, so the page never runs two at once). **Copy lives as constants at the top of the `components/sandspire/about/*` files** (no CMS).
- Contact (`/contact`) - **Cinematic, awwwards-style contact experience** rebuilt to match the `/home-3` + `/services` direction. Sections: a near-black, full-height **WebGL hero** (a bespoke "spire of light" ember field via `ogl`, no Three.js) with the masked word-reveal headline **"Say the word."**, a live "Booking new projects" pill, Start a project + WhatsApp buttons, and a playful drifting channel marquee; a cream **kinetic statement** ("You send one message … a month of ideas worth filming.") revealed word-by-word with inline reel chips; the dark **studio** panel (rounded top) with three tappable direct-contact cards (WhatsApp, Call, Instagram DM), a "replies within 1 business day · Dubai GST" badge, and the **working contact form** in a pointer-reactive gradient-bordered glass card; a cream **FAQ** ("Questions, answered.") reusing the `/home-3` accordion; then the shared site footer. The form still posts to **`/api/contact`** (unchanged). Honours **reduced motion** and pauses the WebGL canvas off-screen. Phone + social links stay CMS-driven (Sanity); the section copy lives in the components, in the `/home-3` voice.
- **Work** (`/work`) - **Cinematic, awwwards-style portfolio page**, rebuilt as a sibling to `/home-3` + `/services` + `/about` (replaces the old Sanity-driven card grid). It presents Sandspire's work as a **curated, playable reel archive** of seven **real** UAE clients (3 Fils, Slrp Ramen, Kanji, Brix Journey, Brix Table, Konbini, Bordo Mavi — using the actual reels/posters in `public/reels/`), **not invented projects**. Sections in order: a **cinematic dark hero** with a **masked headline reveal** ("Reels that earn the walk-in.") + grain/glow + scroll parallax and a meta strip; a dark **capability marquee** ("What one shoot day buys"); **"The archive"** — elegant **category filters** (All / Reels / Photography / Social, with live counts and a sliding pill) where the **first project of the active filter becomes a large cinematic feature card** (its reel autoplays muted in-view with parallax) and the rest fill a **staggered 9:16 reel grid** (poster → muted reel preview on hover, click opens a **full-screen reel lightbox** that plays with sound); a **cursor-following "Watch" preview** pill on desktop; then a **"Let's make you impossible to scroll past."** brand moment + magnetic CTA, and the shared footer. Every card also deep-links to its existing **`/work/{slug}`** case study. Self-contained (no Sanity fetch); uses the global **`SandspireNavBar`** + **`SandspireFooter`** + Lenis smooth scroll; **`motion/react`** only (no GSAP/Three.js, to stay under the Cloudflare Worker size limit); respects **reduced motion** and gates all video playback to on-screen. Project data lives in **`components/sandspire/work/workProjects.ts`** (add a brand by dropping its files in `public/reels/` and appending an entry).
- **Home 3** (`/home-3`) - **Now the main homepage at `/`** (see the **Homepage** entry above). This URL **redirects to `/`** so any old links keep working — there's no separate page here anymore.
- **Brand Strategy** (`/ss-brand-strategy`) - Internal, non-public review page (set to `noindex`) that lays out Sandspire's **new social-first brand strategy**: Sandspire as a **social-media content studio** (reels + photos first) for the UAE brands people check before they visit — **hospitality as the beachhead**, expanding to gyms, salons, hotels and retail — with **web design + AI as secondary** services. Editorial cream-and-orange design with a sticky table of contents, the positioning statement, primary/secondary service hierarchy, the five proof brands (3 Fils, Brix Journey, Brix Cafe, Slrp, Bordo Mavi), homepage messaging direction, a "what to fix" list, and a "what we need from you" checklist. Self-contained (no Sanity fetch) so it always loads instantly. Not linked from the public nav.
- Work project pages (`/work/[slug]`) - Shared layout: dark hero (on **mobile**, text block **above** the hero image; **desktop** image left, copy right), cream **challenge** and **solution** copy (**Jakarta** titles and body text), then a gallery with light hover zoom on images. Content is merged from **Sanity** with code fallbacks in **`lib/workProjectDefaults.ts`**. New projects can exist **only in Sanity** (no code entry) if the document includes a hero image (or path) and the rest of the required fields. No separate “The result” block or wide cover image. (True “case studies” are not modeled yet — that label is reserved for a future content type.)

## Components
- `SandspireNavBar` (`components/sandspire/SandspireNavBar.tsx`) - The **premium global navigation/header**, aligned to the `/home-3` visual system and now **used site-wide on every page**. It's **fixed** and **transparent over the dark hero**, then fades into a **dark blurred-glass bar on scroll**, and **auto-hides on scroll-down / reveals on scroll-up**; a thin **orange scroll-progress hairline** sits under the bar. Desktop links use a **masked "text-roll" hover** (the label rolls up to an orange copy) with a settled orange underline on the active route; the CTA is a **magnetic** orange pill (`Magnetic`). On mobile the hamburger morphs to an X and opens a **full-screen cinematic menu** (ambient orange glow + grain, large indexed display-type links that mask-reveal in, a full-width CTA, social links, and the Dubai location line) with **body-scroll lock, focus trap, Escape-to-close, focus restore, and auto-close if the viewport grows to desktop**. Built on `motion/react` (no GSAP/Three.js); honours reduced motion. Props (`links`, `ctaLabel`/`ctaHref`, `socialLinks`, `logoSrc`/`logoHref`/`logoLoading`, `location`) default from `lib/siteContentDefaults.ts`. The legacy **`SiteNavBar`** and **`SandspireHeader`** are now thin compatibility shims that forward to it (so pages importing them get the new nav).
- `SandspireFooter` (`components/sandspire/SandspireFooter.tsx`) - The **premium global footer**, the cinematic closing brand moment for the `/home-3` system, now **used site-wide on every page**. A near-black desert-night panel with a warm radial glow + grain: an editorial **CTA/brand line** ("Reels, photos and a feed worth following.") with a **magnetic** "Start a project" pill + "See the work" link; three wayfinding columns (**Explore** nav, **Follow** socials with growing-rule **roll-hover** links, **Studio** = location + a **live Dubai GST clock** + a one-line note); a meta row (`© {year} Sandspire · Make them hungry.` + the design credit); and a closing flourish: a **giant cream-to-fade "Sandspire" wordmark** that **parallax-drifts** and bleeds off the bottom edge, with a faint vertical orange "spire" light beam. `motion/react` only; reuses `ScrollReveal`; honours reduced motion; props (incl. `headline`, `blurb`, `logoSrc`, `credit`) default from `lib/siteContentDefaults.ts`. The legacy **`SiteFooter`** and **`SiteFooterFromCms`** now forward to it (mapping the CMS tagline → `headline`, plus `blurb`/`copyright`/`socialLinks`), so every page's footer is editable in Sanity as before.
- `Magnetic` (`components/sandspire/Magnetic.tsx`) - Small reusable wrapper that makes its child **drift toward the cursor on hover and spring back on leave** (the "magnetic button" feel), used by `SandspireNavBar` + `SandspireFooter`. Mouse-only and disabled under reduced motion, so it never fights a tap.
- `HomePageV2` - `/home-2` page shell (hero, work spotlight, service suite, showreel, contact, footer). Sub-pieces: **`HomePageV2WorkScroll`** (Slrp text + scroll videos), **`HomePageV2ServiceSuite`**, **`HomePageV2WorkVideoBackground`** (bright masked glow behind phones only), **`Home2Lenis`** (smooth scroll wrapper).
- `WorkScrollCards` (`components/ui/scroll-card.tsx`) - Reusable sticky stacking card column; used in the **Our Work** section with your three hero side videos. On **desktop**, project copy sits on the left and **glides** sideways when you switch projects; on **mobile**, title/tags/description stick to the top while the videos stack below. **Tap a project tab** or a video card to glide to that project; the active tab has a sliding pill indicator.
- `ContactFAQ` - Dark contact + FAQ; optional **`variant="home2"`**, custom **`faqItems`**, and **`contactMoreQuestionsHref`** for alternate homepages.
- `AgentationProvider` - included by the app layout.
- `ScrollReveal` - optional scroll-into-view fade-up for section content; respects reduced-motion preferences.
- `Home3Hero` + `ReelStoryCard` (`components/sandspire/home3/`) - The `/home-3` hero. `Home3Hero` owns the **drag carousel** of real reels, the fan-out intro, the hover-driven story overlay, playback gating, and the custom cursor; `ReelStoryCard` is one 9:16 reel (autoplays only when it's the focused/hovered card). Reel files live in `public/reels/` — edit the `STORIES` array in `Home3Hero.tsx` to add/remove venues.
- `Home3VideoScroll` (`components/sandspire/home3/Home3VideoScroll.tsx`) - The cinematic "video scroll" section **directly under the statement section on `/home-3`**. A client reel is pinned to the bottom of the screen while three short scenes scroll over it; a centred play button opens a pop-up player of the same reel with sound (closes on ✕ / Escape / backdrop click). Self-contained — no UI library. The background loop uses a **small, muted, web-optimised** encode (`VIDEO_BG_SRC` → `sandspire-showreel-bg.mp4`) while the pop-up plays the **full-quality version with sound** (`VIDEO_SRC` → `sandspire-showreel.mp4`); change either reel via those constants plus `VIDEO_POSTER`, and the scene copy via the `LEFT_SCENES` / `RIGHT_SCENES` arrays at the top of the file.
- `Home3Services` (`components/sandspire/home3/Home3Services.tsx`) - The full-scroll **"What We Do"** section (4th on `/home-3`, directly under the video-scroll section). A tall (500vh) scroll region **pins a screen-height frame**; as you scroll, the headline **What · We Do** rises in, a **sphere blooms outward** from the gap between the words (the container un-clips a fixed-size sphere), a **dashed ring** fades + un-blurs behind the text, and **three service cards** (Reels & short-form · Brand & food photography · Social media, handled) slide up at the corners. Deep near-black background with an orange glow + orange card numbers. Built with `motion/react` (`useScroll`/`useSpring`/`useTransform` — no GSAP); on phones / reduced-motion it becomes a calm stacked layout with everything shown. Edit the `SERVICES` array at the top of the file to change the cards; images are `public/reels/sphere.png` (centre) and `public/reels/ring.png` (the ring, CSS-inverted so its dark strokes read on the dark background).
- `Home3Statement` (`components/sandspire/home3/Home3Statement.tsx`) - The kinetic statement section **directly under the `/home-3` hero**. A big centered sentence ("We turn one shoot day … into a month of reels … that get people off the app … and through the door.") that **reveals one word at a time**, each fading from light cream-grey to near-black, while **three small rounded reel thumbnails "pop" in** between the words. Plays once when it scrolls into view (and on load, since it sits just under the hero). To change the words or the inline pictures, edit the `TOKENS` list at the top of the file; the thumbnails reuse the reel posters in `public/reels/posters/`. Honours reduced-motion (shows the finished sentence with no animation).
- `ContactAurora` · `ContactHero` · `ContactStatement` · `ContactStudio` (`components/sandspire/contact/`) - The `/contact` page pieces. **`ContactAurora`** is the bespoke `ogl` WebGL hero backdrop (a "spire of light" ember shader; caps DPR, damps the pointer, pauses off-screen, paints one static frame under reduced motion). **`ContactHero`** owns the masked word-reveal headline ("Say the word."), the availability pill, CTAs and the channel marquee. **`ContactStatement`** is the cream word-by-word kinetic reveal (edit the `TOKENS` array to change the copy/chips). **`ContactStudio`** holds the direct-contact cards + the reused **`ContactForm`** in a pointer-reactive gradient card; to change the studio headline/intro, edit the props passed in `app/contact/page.tsx`. To swap the hero look, edit the fragment shader in `ContactAurora.tsx`.
- **`/services` components** (`components/sandspire/services/`):
  - **`ServicesHero`** + **`EmberField`** - the hero. `EmberField` is a self-contained **WebGL** backdrop written from scratch with **`ogl`** (already a project dependency, so it adds no new bundle): a single full-screen fragment shader of fractal-noise "embers" in brand orange on near-black, with a soft glow that follows the pointer. It caps DPR, damps the pointer, **pauses its render loop off-screen / when the tab is hidden**, and draws one calm frame under reduced-motion. `ServicesHero` lays the headline, CTAs, scroll cue and a drifting service marquee over it (with a CSS-gradient fallback if WebGL is unavailable).
  - **`ServiceIndex`** - the centerpiece: a cursor-tracking video-reveal list of the five **content** services. On a real pointer (desktop) hovering a row floats that service's actual reel (`public/reels/*.mp4`, one video mounted at a time) toward the cursor with a damped spring while the other rows dim; on touch / reduced-motion it degrades to a stacked list with poster thumbnails. Edit the `ITEMS` array to change the services/reels.
  - **`ServicesStatement`** - the kinetic word-reveal thesis (same mechanic as `Home3Statement`, new copy; edit `TOKENS`).
  - **`ServicesSecondary`** - the cream grid of the four secondary services (web, brand, SEO, AI). Edit `CARDS`.
  - **`ServicesProcess`** - the horizontally-pinned process rail. A tall scroll driver pins a screen-height frame and slides the row of step cards sideways (slide distance measured from the real track width); on mobile / reduced-motion it's a vertical stacked grid. **Note:** the `<section>` must NOT be `overflow-hidden` (that breaks `position: sticky`); only the sticky frame and track wrapper clip. Edit `STEPS`.
  - **`ServicesFaq`** - the services-scoped FAQ accordion (same styling as `Home3Faq`; edit `FAQS`).
- **`/about` components** (`components/sandspire/about/`):
  - **`SandField`** (`.jsx`) - the shared **WebGL** backdrop for `/about`, written with **`ogl`** (no new bundle): a domain-warped fractal-noise "sand drifting in wind" fragment shader in brand ember/orange on near-black, with a cursor-following warm glow. Caps DPR at 2, **pauses off-screen** (IntersectionObserver), paints **one still frame** under reduced motion, and falls back to a CSS radial gradient if WebGL is unavailable. Used by both the hero and the name moment. Props: `className`, `intensity`.
  - **`AboutHero`** - the hero: `SandField` + a contrast wash + grain under the eyebrow ("About Sandspire · Dubai, UAE") and the bottom-anchored headline **"The studio behind the feed."** Edit the headline/description inline.
  - **`AboutManifesto`** - the cream kinetic "Why we exist" line; reveals word-by-word with an orange payoff. Edit the `TOKENS` array (set `accent: true` to colour a word orange).
  - **`AboutStats`** - the dark **count-up** strip; each figure eases from 0 on scroll-in (instant under reduced motion). Edit the `STATS` array (`to`, `suffix`, `label`, `sub`).
  - **`AboutPrinciples`** - the cream interactive "What we believe" list (rows warm to orange + nudge right on hover; descriptions always shown on mobile). Edit the `PRINCIPLES` array.
  - **`AboutShowcase`** - the dark proof section: a responsive grid of live client reels (`public/reels/*.mp4`) + a scrolling capabilities marquee. Edit the `REELS` and `CRAFTS` arrays.
  - **`AboutNameMoment`** - the giant **"From sand, a spire."** brand moment over the live `SandField` (which **lazy-mounts** ~400px before the section, so two shaders never run at once). Edit the headline words / body copy inline.
- **`/work` components** (`components/sandspire/work/`):
  - **`workProjects.ts`** - the **single source of the portfolio**: a typed `WORK_PROJECTS` array (id, name, handle, `slug` → `/work/{slug}`, place, category, `disciplines` → drives the filters, year, blurb, logo, poster, reel) plus `WORK_FILTERS` and `countForFilter()`. **Add a brand** by dropping `reel`/`poster`/`logo` in `public/reels/` and appending an entry — order matters (the first entry is the default lead feature).
  - **`WorkHero`** - the dark cinematic hero: masked line-reveal headline ("Reels that earn the walk-in."), warm glow + grain, a scroll-parallax/fade on the text block, a "recording" pulse on the eyebrow, a meta strip, and a desktop-only scroll cue. Edit the headline lines / `META` inline.
  - **`WorkGallery`** - the orchestrator (client): holds the **filter** + **lightbox** state, renders the section head, the **filter pills** (sliding `layoutId` indicator, live counts), the **feature + staggered grid** (cross-faded on filter change), the **cursor-following "Watch" preview** pill (desktop/pointer-fine/motion-on), and mounts the `ReelLightbox`.
  - **`WorkFeature`** - the large lead case-study cover for the first project of the active filter: big "01 / FEATURED", title/place/blurb, "Watch the reel" + "Full case study" CTAs, and a 9:16 reel that **autoplays muted while in view** (IntersectionObserver) with a scroll parallax + hover scale.
  - **`WorkProjectCard`** - a 9:16 grid card: poster that **cross-fades to the muted reel on hover/focus** (`preload="none"`, one plays at a time), number/year/category/title/place overlay + center play affordance, then an editorial blurb + ↗ case-study link below. Click opens the lightbox.
  - **`ReelLightbox`** - the full-screen reel player (reused pattern from `Home3VideoScroll`): plays the vertical reel **with sound**, story-chrome header (avatar + handle), category/year + "Full case study" deep link; Esc / backdrop / button close, body-scroll lock, reduced-motion-aware scale-in.

## Sanity CMS (editable content)
The site is wired for [Sanity](https://www.sanity.io/). **The live site does not ship the full Studio UI** (that bundle is too large for Cloudflare’s server limits). **Edit content** in [Sanity Manage](https://www.sanity.io/manage), or run the full **Studio** on your computer: in the project folder use **`npx sanity dev`**, then open the URL it prints (the config is still at **`/studio`**). On the **public site**, **`/studio`** is a small page with the same links and instructions.

**Local setup**
1. Copy **`.env.example`** to **`.env.local`**. Your project is **`1fmk53vd`** / dataset **`production`** (Sandspire in [Sanity Manage](https://www.sanity.io/manage/project/1fmk53vd)).
2. **Do not** run `npm create sanity@latest` again in this repo — Sanity is already set up. That command is for brand-new projects and can overwrite config.
3. Check the connection anytime: **`npm run verify:sanity`** (should show all ✓ checks).
4. For the **editing UI**, run **`npm run sanity:dev`** (or `npx sanity dev`) and sign in with your Sanity account.
5. In Sanity **Manage** → **API** → **CORS origins**, add `http://localhost:3000` (and your production URL when you deploy).

**Deploy:** Add the same `NEXT_PUBLIC_SANITY_*` variables to your host (e.g. Cloudflare **`.dev.vars`** / dashboard env) so builds don’t fail. **Cloudflare:** the Worker script must stay under the host’s size limit; on the **free** plan that limit is very small, so a **paid Workers** plan (larger limit) is usually needed for this OpenNext app, or use another host (e.g. Vercel) for production.

**Code:** Client in `sanity/lib/client.ts`, config in `sanity.config.ts`, schemas in `sanity/schemaTypes/`. Page copy is loaded through **`sanity/lib/queries/siteContent.ts`** with fallbacks in **`lib/siteContentDefaults.ts`**.

### What you can edit in Sanity (almost everything)

Open **`npx sanity dev`** (or Sanity Manage) and use the **Content** list on the left:

| Document | What it controls |
|----------|------------------|
| **Site settings** | Site name & description (browser tab), phone, **navigation links**, header “Get in touch” button, **footer** tagline/blurb/copyright, **social links**, contact headline/intro, **FAQ** (default + home-2 versions) |
| **Homepage (version 1)** | Main page hero text & video, service labels under hero, “Who is Sandspire”, services bento titles/prices, featured case studies, analytics video paths |
| **Homepage (version 2)** | `/home-2` hero, Our Work scroll items, Service Suite cards & bento images, showreel, page title for SEO |
| **About page** | All text on `/about` |
| **Work listing page** | Headline, subheadline, and empty-filter message on `/work` |
| **Work projects** | Each project page + cards on `/work` (same as before) |
| **Client logos** | Logo strip on both homepages |

After you **Publish** in Studio, the live site picks up changes within about **one minute** (`revalidate = 60`). If Sanity is slow or offline, the site keeps showing the built-in defaults from **`lib/siteContentDefaults.ts`**.

**Seed all site copy at once (recommended first time):**
1. In [Sanity Manage](https://www.sanity.io/manage) → your project → **API** → **Tokens**, create an **Editor** token.
2. Add **`SANITY_API_WRITE_TOKEN`** to **`.env.local`** (this is only for *uploading* content — visitors do **not** need it).
3. Run **`npm run seed:sanity-site-content`** — uploads text **and** home-2 images (hero + bento photos) into Sanity’s CDN.
4. Run **`npm run seed:sanity-work-projects`** if work projects are not in Sanity yet.
5. Open **`npx sanity dev`** → **Homepage (version 2)** to swap images anytime.

**Where home-2 images live in Sanity:** Studio → **Homepage (version 2)** → **Hero image**, **Service bento — cocktail photo**, **Service bento — food photo**. They are stored on **`cdn.sanity.io`** (not in your `public/` folder). You do **not** need a separate image API key to *show* them on the site — only the normal **`NEXT_PUBLIC_SANITY_*`** vars (already in `.env.example`).

**Still fixed in code (not in CMS yet):** contact form field labels and some layout-only styling. **All photos and videos** are in **Sanity** (served from `cdn.sanity.io`); the **`public/` folder** is intentionally empty — use the seed commands below if you need to re-upload from git history.

### Work projects in Sanity
Each URL **`/work/{slug}`** loads a **work project** document in Sanity (type **`workProject`**; older **`caseStudy`** docs are still read until migrated). If the document is missing or a field is empty, the site uses **`lib/workProjectDefaults.ts`** for that slug.

**Seed all eight documents at once (recommended):**
1. In [Sanity Manage](https://www.sanity.io/manage) → your project → **API** → **Tokens**, create an **Editor** token.
2. In the project root, set **`SANITY_API_WRITE_TOKEN`** in **`.env`** or **`.env.local`**. (Optional: add `NEXT_PUBLIC_SANITY_*` from **`.env.example`** if you use a different project or dataset; otherwise the same defaults as **`sanity/env.ts`** are used.) The seed script loads those files with **`dotenv`**.
3. Run **`npm run seed:sanity-work-projects`** (or **`npm run seed:sanity-case-studies`**, same script). This upserts documents with IDs `workProject-{slug}`, filled with the same text as the code defaults (replace placeholder `https://example.com` URLs in Studio when you have real links).

**Or create one by hand in Studio:** **Create** → **Work project** → **Slug** must match the route (e.g. `slrp`, `3-fils`, `brix-journey`). **Publish**, then reload the page ( **`revalidate = 60`** ).

**Adding a new project later:** Add an entry to **`WORK_PROJECTS`** in `lib/workProjectDefaults.ts` if you want a code fallback, or create only a **Work project** in Sanity (or both). The **`/work`** listing is driven from Sanity; re-run the seed if you use it to sync from code defaults.

## Recent Changes
- 2026-06-23: **Made the social-first `/home-3` design the main homepage at `/`.** The cinematic reel-carousel concept now loads when anyone visits the site root. **How:** moved the full page (content + the page-scoped animation CSS) from `app/home-3/page.tsx` into **`app/page.tsx`**, and wrapped it in **`Home2Lenis`** *inside the page* so it keeps its smooth scroll (the root layout has no Lenis, and a page can't inherit a sibling route's layout) — `app/home-3/layout.tsx` was deleted as a result. The page keeps its tailored `metadata` (title/description), which overrides the Sanity-driven defaults from the root layout for `/`. **Preserved the old homepage** at **`/home-1`** (`app/home-1/page.tsx`, the original Sanity/CMS-driven Hero + bento + case-studies design) so nothing was lost and the two can be compared — it's not linked from the nav. **`/home-3` now redirects to `/`** (`app/home-3/page.tsx` is a one-line `redirect("/")`) so any old links keep working without serving duplicate content. No nav changes were needed: the navbar logo already points at `/`. Verified **typecheck clean** (`tsc --noEmit`, 0 errors) and that every imported component resolves.
- 2026-06-23: **Redesigned `/work` into a cinematic, awwwards-style portfolio** (it was a Sanity-driven card grid). Rebuilt as a sibling to `/home-3` + `/services` + `/about`, presenting the work as a **curated, playable reel archive** of the **seven real UAE clients** already in `public/reels/` (3 Fils, Slrp Ramen, Kanji, Brix Journey, Brix Table, Konbini, Bordo Mavi) — deliberately **not** the invented real-estate "projects" from the brief, since Sandspire is a content studio. New route `app/work/layout.tsx` (Lenis) and a component set in `components/sandspire/work/`: **`workProjects.ts`** (typed data + filters), **`WorkHero`** (masked headline reveal + grain/glow + scroll parallax), **`WorkGallery`** (filter + lightbox orchestrator: sliding-pill **category filters** with live counts, a cross-faded feature+grid, and a desktop **cursor-following "Watch" preview**), **`WorkFeature`** (large lead case-study cover whose reel autoplays muted in-view with parallax), **`WorkProjectCard`** (9:16 card, poster → muted reel preview on hover, opens the lightbox), and **`ReelLightbox`** (full-screen reel player **with sound** + story chrome + `/work/{slug}` deep link, Esc/backdrop/scroll-lock). The lead feature is **whichever project sits first in the active filter**, so the filters drive both the grid and the hero of the section. Each card also deep-links to its existing **`/work/{slug}`** case study. **Replaced** the old `app/work/page.tsx` (it no longer fetches Sanity; `WorkProjectGrid`/`getWorkIndexCards`/`getWorkPageSettings` are simply no longer imported here, and the `/work/[slug]` detail route is untouched). Built on the existing **`motion/react`** + **`lenis`** stack — **no GSAP/Three.js** (Cloudflare-Worker-size reasoning, same as the other pages); all video playback is **gated to on-screen** and reduced motion is respected throughout. Verified in headless Chrome at **1440 / 834 / 390 px**: masked hero reveal, capability marquee, filters (counts All 7 · Reels 6 · Photography 3 · Social 4; pill slides; feature swaps to the new lead), the grid reveals on scroll, the lightbox plays a real reel with sound and **Esc closes it + restores scroll**, mobile stacks to full-width reel cards (scroll-cue hidden so it no longer collides with the meta strip), tablet uses a 2-col grid — **no horizontal overflow, 0 console errors**; **typecheck + ESLint clean**. The lone dev console note is the **pre-existing site-wide** Framer `useScroll` "non-static position" warning (identical on `/home-3`), left as-is.
- 2026-06-23: **Rolled the new `SandspireNavBar` + `SandspireFooter` out site-wide** (they were live only on `/home-3`). Every page now shows the premium fixed nav + cinematic footer. **How:** the old shared components were turned into thin compatibility shims — **`SiteNavBar`** and **`SandspireHeader`** now forward to `SandspireNavBar`, and **`SiteFooter`** (and therefore **`SiteFooterFromCms`**) forwards to `SandspireFooter` (mapping the CMS tagline → footer `headline`, plus `blurb`/`copyright`/`socialLinks`/`logoSrc`), so all CMS-driven footer content stays editable in Sanity. The five pages that imported `SiteNavBar` directly were migrated explicitly (mirroring the home-3 pattern): **`/`** (`Hero.tsx`), **`/home-2`** (`HomePageV2.tsx`), **`/about`**, **`/contact`** (keeps its `#start` CTA anchor), **`/services`**; the about + services **inline footers** were replaced with `<SandspireFooter>`. **Layout care (the nav is now `fixed`, not in-flow):** on the homepage the hero's `[transform:translateZ(0)]` would have trapped a fixed child, so the nav is rendered as a sibling **outside** that container (fragment); `/home-2`'s old `-mt-[54px]/pt-[54px]` nav-compensation was removed; and `/work/[slug]`'s hero top padding was bumped (`pt-8/md:pt-12` → `pt-24/md:pt-28`) to clear the overlay nav. Every page is dark-themed at the top, so the transparent-over-hero state stays legible everywhere (work/coming-soon already had `pt-20`/`py-24` clearance). The CMS-default footer tagline ("Creative that / pulls its weight.") still flows through, so the **web-focused `/` and the social-focused `/home-3` keep their own taglines** by design. Verified: all 8 routes return **200**; each route's server-rendered HTML contains both the nav (`aria-controls="ss-mobile-menu"`) and footer (`The last word`, `Explore`, live `GST` clock) markers; full **typecheck + ESLint clean** across the 9 touched files. (Visual screenshots not re-captured this pass — the Playwright MCP browser was wedged on a dead process; the nav/footer themselves were already screenshot-verified on `/home-3` at 1440px + 390px.)
- 2026-06-23: **Rebuilt the global Navigation + Footer to match the `/home-3` design system** (new premium components, wired into `/home-3`). Created **`components/sandspire/SandspireNavBar.tsx`**, **`components/sandspire/SandspireFooter.tsx`**, and a shared **`Magnetic.tsx`** helper, then swapped them into `app/home-3/page.tsx` (replacing the old `SiteNavBar` usage and the page's inline `<footer>`). **Nav:** fixed + transparent over the dark hero → **dark blurred-glass on scroll**, **auto-hide on scroll-down / reveal on scroll-up**, an orange **scroll-progress hairline**, **masked text-roll** link hovers + active-route underline, a **magnetic** orange CTA pill, and a **full-screen cinematic mobile menu** (indexed display-type links that mask-reveal, full-width CTA, socials, Dubai line) with **scroll-lock + focus trap + Esc-close + focus-restore + auto-close on resize-to-desktop**. **Footer:** an atmospheric desert-night closing moment — editorial brand line + magnetic CTA, three wayfinding columns (Explore / Follow / Studio with a **live Dubai GST clock**), a meta row, and a **giant cream-to-fade "Sandspire" wordmark** that **parallax-drifts** and bleeds off the bottom with a faint orange "spire" beam. **Decisions:** built on the existing **`motion/react`** stack (magnetic pulls, masked rolls, parallax, springs) — **no GSAP/Three.js**, matching the rest of the codebase and the Cloudflare-bundle reasoning; and shipped as **new** prop-driven components (defaults from `lib/siteContentDefaults.ts`) rather than editing the shared `SiteNavBar`/`SiteFooter`, so the other 6+ pages that use those stay untouched. All motion honours reduced motion. Verified in headless Chrome at **1440px and 390px**: nav glass/auto-hide/progress work, the footer + live clock + wordmark render, the mobile menu opens with body-scroll-lock and closes on Escape with focus restored to the toggle, **no horizontal overflow, 0 console errors**; typecheck + ESLint clean for the new files. (The one remaining dev console note — Framer's `useScroll` "non-static position" message — is the **pre-existing** one from `Home3Services`; both new components target `position: relative` elements.) These are **global-ready**: to roll them out site-wide later, swap `SandspireHeader`/`SiteFooter` usages on the other pages for `SandspireNavBar`/`SandspireFooter`.
- 2026-06-23: **Redesigned `/about` into a cinematic, awwwards-style studio page** (it was an all-black two-column Sanity page). Rebuilt as a sibling to `/home-3` + `/services` + `/contact`: cream/near-black rhythm, Geist + Plus Jakarta, single `#FF5E00` accent, `motion/react` reveals + Lenis smooth scroll. New route layout `app/about/layout.tsx` (Lenis) and a component set in `components/sandspire/about/`: **`SandField`** (a featherweight `ogl` **WebGL** "drifting sand/ember" fragment shader — the name *Sandspire* made literal — cursor-reactive, DPR-capped, off-screen-paused, one-frame under reduced motion, CSS-gradient fallback), **`AboutHero`** ("The studio behind the feed."), **`AboutManifesto`** (word-by-word kinetic line with an orange payoff), **`AboutStats`** (count-up operating-model numbers), **`AboutPrinciples`** (interactive "five rules" list), **`AboutShowcase`** (live client reels + capabilities marquee), and **`AboutNameMoment`** ("From sand, a spire." over the live shader, which **lazy-mounts** so two shaders never run at once). **Deliberately used the existing `motion/react` + `lenis` + `ogl` stack — no GSAP or Three.js — to stay inside the Cloudflare Worker size limit** (same reasoning as `/home-3`, `/services`, `/contact`). Self-contained: copy lives as constants in the components (the old Sanity-driven About copy is no longer used); uses the stable shared **`SiteNavBar`** + an inline footer (matching how `/contact` kept the stable nav). Verified in headless Chrome at **1440px and 390px**: WebGL hero renders, every section reveals, reels play, **no horizontal overflow**, and **0 console errors in normal mode**. Fixed two bugs found during QA: a **count-up hydration mismatch** (the counter seeded its initial state from `useReducedMotion`, which differs server↔client — now always starts at 0 and resolves in an effect) and a **reduced-motion risk in the hero** (spreading `false` dropped the `animate` prop, which could leave the headline transparent — now `initial`/`animate` are static and only the transition timing is gated). The lone remaining reduced-motion hydration warning is **pre-existing and site-wide** (from the shared `ScrollReveal`, which swaps `motion.div`↔`div`): `/home-3` and `/` emit the identical warning, so it was left as-is rather than diverging the shared component.
- 2026-06-23: **Redesigned `/contact` into a cinematic, awwwards-style experience** (it was a thin reuse of the shared `ContactFAQ`). Rebuilt to match the `/home-3` + `/services` direction: cream/near-black rhythm, Geist + Plus Jakarta, single `#FF5E00` accent. New components in `components/sandspire/contact/`: **`ContactAurora`** (a bespoke `ogl` WebGL hero backdrop — a "spire of light": domain-warped ember curtains rising into a soft central column, with a pointer-tracking bloom; **no Three.js**, same Cloudflare-bundle reasoning as `/home-3` and `/services`), **`ContactHero`** (masked word-reveal headline **"Say the word."**, live "Booking new projects" pill, Start a project + WhatsApp CTAs, a witty drifting channel marquee, scroll cue), **`ContactStatement`** (cream word-by-word kinetic reveal with inline reel chips: "You send one message … a month of ideas worth filming."), and **`ContactStudio`** (the dark form panel: three tappable direct-contact cards — WhatsApp, Call, Instagram DM — a "replies within 1 business day · Dubai GST" badge, and the existing **`ContactForm`** wrapped in a pointer-reactive gradient-bordered glass card). The page (`app/contact/page.tsx`) stays a server component: it keeps the Sanity-driven phone + social links and metadata, derives the `wa.me` link from the phone, and closes with a cream **FAQ** (`Home3Faq`, "Questions, answered.") + the shared `SiteFooterFromCms`. **The contact form still posts to `/api/contact` unchanged** (validation + Resend/webhook untouched). All motion honours reduced-motion and the WebGL canvas pauses off-screen. Verified in headless Chrome (CDP) at **1440px and 390px**: WebGL renders, every section reveals, the form + channels are present, **no horizontal overflow, 0 console errors**. Fixed one bug during QA: an `ogl` `uniform2fv` size mismatch in `ContactAurora` (`iResolution` declared `vec2` but passed a 3-float array → now a true `vec2`). Section copy lives in the components in the `/home-3` voice (not Sanity), so to reword the hero/statement edit those files. **Note:** built alongside concurrent edits to the repo (a parallel `/about` + `/services` redesign that renamed the nav/footer to `SandspireNavBar`/`SandspireFooter`); `/contact` deliberately still uses the stable shared `SiteNavBar` + `SiteFooterFromCms`.
- 2026-06-23: **Designed and built a brand-new `/services` page — an awwwards-style services landing page** (the route didn't exist before). Built as a sibling to `/home-3`: same cinematic cream/near-black rhythm, Geist + Plus Jakarta, single `#FF5E00` accent, `motion/react` scroll reveals and Lenis smooth scroll. New route `app/services/` (page + Lenis layout) and a component set in `components/sandspire/services/`. Sections: a **bespoke WebGL "ember field" hero** (a custom `ogl` fragment shader — orange embers smouldering on near-black, cursor-reactive — see `EmberField`), a **kinetic word-reveal thesis**, an interactive **cursor-tracking video-reveal "content studio" index** (hovering a service floats its real client reel toward the cursor; the page centerpiece), a cream **"build & automation studio"** grid (web/brand/SEO/AI, numbered 06–09), a **horizontally-pinned process scroll** (Brief→Plan→Shoot→Publish→Report), a **"Ways to work"** band, a **"Get seen. Get visited."** brand moment, and a **CTA + services FAQ + footer**. Copy follows BRAND-VOICE.md (social-first, breadth beyond food, dry and specific). **Deliberately used the existing `motion/react` + `lenis` + `ogl` stack — no GSAP or Three.js — to respect the Cloudflare Worker bundle limits** (the same reason `/home-3` avoids them); `ogl` gives a real GPU-driven hero at zero added bundle cost. The top-nav **"Services"** link now points to `/services` (was `/#services`; default in `lib/siteContentDefaults.ts`, overridable in Sanity). Verified in-browser (Chrome via Playwright) at **1440px and 390px**: WebGL hero renders, the hover-reveal centerpiece works, the pinned process pins and slides, **no horizontal overflow, 0 console errors**; typecheck + ESLint clean (0 errors). Fixed two bugs found during QA: an `ogl` `uniform2fv` size mismatch (declared `iResolution` as `vec2` but passed a 3-float array → now `vec3`), and a broken pin (the process `<section>` had `overflow-hidden`, which disables `position: sticky` — moved the clip to the sticky frame/track only).
- 2026-06-23: **Swapped the `/home-3` video-scroll (3rd section) background to the new Sandspire showreel, and optimised it for the web.** Replaced the old `konbini` clip with the owner's `sandspire-showreel.mp4`, encoded **two ways** (Next.js video best practice for a "background loop + click-to-expand" pattern): (1) `public/reels/sandspire-showreel-bg.mp4` — the **sticky background loop**: muted (its audio was never played anyway), scaled to **720p** since it sits behind a 55%-black overlay, and **`+faststart`** so it begins playing before it finishes downloading; **3.7 MB → 1.4 MB (≈63% smaller)**. (2) `public/reels/sandspire-showreel.mp4` — the **full 1080p + audio version** for the pop-up player, re-muxed with `+faststart` (no quality loss); it only loads when someone clicks play. Also added a **poster frame** (`public/reels/posters/sandspire-showreel.jpg`) so an image shows instantly before the video paints. Wired via `VIDEO_BG_SRC` / `VIDEO_SRC` / `VIDEO_POSTER` and `preload="auto"` on the background in `Home3VideoScroll.tsx`. The `konbini` reel is still used by the hero (`Home3Hero.tsx`), so it was left in place.
- 2026-06-23: **Rewrote the `/home-3` kinetic statement sentence** (owner didn't like the old one). Changed "…that bring people in, not vanity likes." to **"We turn one shoot day → a month of reels → that get people off the app and through the door."** Sharper, wittier payoff ("off the app" implies the anti-vanity point without the cliché; "through the door" works for any visit-driven brand, not just food), and the **third reel chip now lands after "app"** instead of awkwardly splitting "not [chip] vanity likes." Edited the `TOKENS` array in `Home3Statement.tsx`. Verified `/home-3` returns 200.
- 2026-06-23: **Made the `/home-3` headlines bolder** (owner: they read too thin). Stepped every section headline two weights up, from Geist **medium (500) → bold (700)** (done in two passes: medium→semibold, then semibold→bold) — the hero "If it's worth visiting…", the kinetic statement, the video-scroll scene headings, the "What We Do" big words + service-card titles, "Plan. Shoot. Post. Report." and its step names, "Make them hungry.", and the final-CTA headline. Geist is a variable font so no new font files load. **Deliberately left at medium:** the decorative process numbers (01–04), the footer "Sandspire" wordmark, and the small reel-card name labels, to keep the size/weight contrast intentional. Files: `app/home-3/page.tsx` and `components/sandspire/home3/{Home3Hero,Home3Statement,Home3VideoScroll,Home3Services}.tsx`. The headlines now use `font-bold`; to dial it back, swap to `font-semibold` (600).
- 2026-06-23: **Broadened `/ss-brand-strategy` beyond food to match the new `/home-3` direction** (owner request). Reframed the whole strategy from "social studio for UAE hospitality/F&B" to "content studio for the UAE brands people check before they visit," with **hospitality as the beachhead** (where the real proof and cited research live) and an explicit **expansion path** to adjacent visit-driven verticals (gyms, salons, clinics, hotels, retail). Updated: metadata, hero subhead and focus chip (Niche became **Beachhead**), one-line pitch, all three positioning statements (Part I §03, sharpened v2.1, locked L1), audience, the Proof section (now "the hospitality beachhead" with a beachhead/expansion intro), brand-expression and diagnosis copy, taglines (primary functional broadened; secondary emotional is now the live hero line **"If it's worth visiting, it's worth filming."**), the "Make them hungry." community-line rationale (kept, reframed as figurative), and the 12-week theme note. Also fixed two drifts: removed the banned cliché **"scroll-stopping"** from the live positioning copy (kept only in the "avoid/drop" lists), and replaced the **killed "3 free reel ideas" offer** everywhere it lingered (IMPLICATIONS CTA, the text-message voice example, theme wk11) with the "show us your brand, then a plan and a quote" framing. **Left intact on purpose:** the Part II competitor/evidence/pricing research, which is the cited F&B-beachhead data, now framed as such rather than rewritten. Verified `/ss-brand-strategy` returns 200.
- 2026-06-23: **Removed every em dash (—) from the visible copy on `/home-3`** (owner request). Reviewed all the reader-facing text across the page and replaced each em dash with cleaner punctuation chosen per sentence — a period for a clean break ("…before it goes live. No surprises on your feed."), a comma for a continuing aside ("…talking-to-camera video, captured while you carry on with the day."), or a colon where the dash introduced a list/definition ("The whole feed off your plate: strategy, captions, scheduling…"). Touched the metadata title/description plus the Process, brand-moment and footer copy in `app/home-3/page.tsx`, the four FAQ answers in `Home3Faq.tsx`, the scene copy in `Home3VideoScroll.tsx`, the hero description in `Home3Hero.tsx`, the three service cards in `Home3Services.tsx`, and the screen-reader label in `ReelStoryCard.tsx`. **Left untouched on purpose:** em dashes inside *code comments* (developer notes, not copy) and the en dash (–) in the FAQ price range "AED 5–15k" (correct typography for a range, not an em dash).
- 2026-06-23: **Reworked the `/home-3` final CTA so it no longer offers free reel ideas** (owner: the "three reel ideas back" promise can be abused — we don't do free reel concepts). Headline changed from "Show us your brand. We'll send three reel ideas back." to **"Show us your brand. Let's talk about what we'd shoot."**; the body now ends "…we'll come back with **a plan and a quote**." (was "three reels we'd actually shoot first"); and the button is **"Start a project"** (was "Get 3 reel ideas"), matching the site's primary CTA. In `app/home-3/page.tsx`. Verified `/home-3` returns 200.
- 2026-06-23: **Changed the `/home-3` hero headline** from "Make them hungry." to **"If it's worth visiting, it's worth filming."** (owner request) in `Home3Hero.tsx`. Resized it to a clean two-line statement (`text-[clamp(2rem,4.8vw,3.6rem)]`, slightly looser leading/tracking, manual `<br>` at the comma) since it's longer than the old three-word line. Also reworded the video-scroll **"Built for more than restaurants"** scene so it no longer repeats the new hero line (was "…if it's worth visiting, it's worth filming properly" → "Different places, same job: make people want to walk in."). **Note:** "Make them hungry." still appears as the large **brand moment** lower on the page and the **footer signoff** ("© Sandspire. Make them hungry.") — left in place for now; can be changed too on request. Verified `/home-3` returns 200.
- 2026-06-23: **Full copywriting pass on `/home-3` — broadened beyond food, cut repetition, added personality** (owner request: "very repetitive… no personality… too focused on food… expand to different verticals"). **(1) De-restauranted the language across the whole page** so Sandspire reads as a UAE content studio for *any* brand people check before they visit (restaurants, gyms, salons, clinics, hotels, shops) — food is one flagship vertical, not the identity. Reworked the food-locked *metaphors*, not just nouns: "fill real tables" → "bring people in", "your floor" / "the room" / "best rooms" gone, "your dishes" / "the kitchen" → "your brand and your space", "food photography" → "photography" / "Brand & product photography", "bookings" → "demand" / "the business". **(2) Killed cross-section repetition** — the *statement*, *video-scroll* scenes and *services* cards were each repeating the same three ideas (one-shoot-day, photos-that-sell, feed-handled); the video-scroll and services even shared the literal line **"Plan, shoot, post, reply, report"**, which is *also* the Process headline. Now each section does a distinct job: statement = the core transformation, video-scroll = why content matters / breadth across verticals / it's handled, services = the concrete service list. **(3) Added personality** in the brand voice (dry, specific): "everyone in between", "the thing that talks them out of it", "the one most brands can't keep up with. We can.", "whatever you sell, shot to sell it", "not a dashboard you'll never open." **(4) Kept the signature "Make them hungry." line but reframed its supporting copy figuratively** (hungry to *try it, book it, tell a friend* = desire/demand) so it stretches across verticals instead of meaning literal food. **(5) New FAQ "Do you only work with restaurants?"** answers the breadth question head-on. **(6)** Hero category line → "UAE content studio · reels, photos, social"; final-CTA headline → "Show us your brand…"; metadata title/description, video-scroll scenes, and footer blurb all broadened. Files: `app/home-3/page.tsx`, `components/sandspire/home3/{Home3Hero,Home3Statement,Home3VideoScroll,Home3Services,Home3Faq}.tsx`. Verified: `/home-3` returns 200 (page compiles + renders). Note: this consciously diverges from the food-first framing in `/ss-brand-strategy`, which may want updating to match the broader direction.
- 2026-06-23: **Removed five sections from `/home-3` at the owner's request** (in `app/home-3/page.tsx`). Cut the editorial **problem** section ("Your room is full of energy. Your feed isn't."), the **Sandspire difference** (4-differentiator) section, the static **"What we do" services grid** ("One studio for the whole feed." — note the cinematic full-scroll **`Home3Services`** "What We Do" reveal is kept; only the static card grid was removed), the cinematic **reel proof-wall** ("Real venues. Real reels."), and the **pricing** section ("No hidden retainers. Just real numbers."). The page now runs: hero → statement → video-scroll → cinematic "What We Do" reveal → brand marquee → process → "Make them hungry." → final CTA + FAQ → footer. Also removed the now-unused data arrays (`DIFFERENTIATORS`, `PRIMARY_SERVICES`, `PROOF`, `PRICING`) and the `ReelCard` import, and fixed the footer nav (its `#services` / `#reels` / `#pricing` links pointed at the deleted sections — now just **The work** → `/work` and **Contact** → `/contact`). The shared top nav is unchanged (it points at the real homepage, not `/home-3`). Verified: ESLint passes with 0 errors. The `ReelCard` component file is kept in `components/sandspire/home3/` in case it's reused later.
- 2026-06-23: **Copywriting pass on `/home-3` (P1 + P2 fixes, using PM-skills frameworks).** Sharpened the page against value-proposition (JTBD), positioning, value-prop-statement, and grammar/flow checks. Changes: (1) **Hero now names the category** — added an orange eyebrow **"UAE reels & food-photography studio"** above the **"Make them hungry."** headline (in `Home3Hero.tsx`), and bumped the hero description from 65%→75% opacity so what we *do* is legible, not just the mood line. The big text stays a real `<h1>` for SEO. (2) **Proof / reel-wall rewritten to be honest** — it previously promised real view counts while showing placeholders, which contradicted the "name + reel + number" promise. Intro now reads "…the first reels are in production — real footage and real view counts land here as each one goes live," each card's metric pill says **"Reel in production"** (was "Add real views"), and the footnote reframes the placeholders honestly (in `app/home-3/page.tsx`). (3) **De-duplicated the "vanity" theme** — it appeared 5+ times; kept the anchor line in the statement section and varied the differentiator #04, process #04 (Report), and problem stat-card copy so each adds something new. (4) **Plain-language for restaurateurs** — "UGC & creator content" → **"Creator-style video (UGC)"** with a plainer description; removed the unsupported **"highest ROI in 2025"** claim in both the services card and `Home3Services.tsx` ("the format restaurants are pulling the most reach from right now"); "scroll-ready assets / margin lever" → "ready-to-post reels / most cost-effective way to keep a feed busy." (5) **CTA microcopy** — the final CTA button now says **"Get 3 reel ideas"** (was "Start a project") to echo the "send your venue, we'll send three reel ideas back" offer. Verified: `/home-3` returns 200 and all new copy renders server-side. **Still needed from you:** the five real reels + real view counts to replace the proof-wall placeholders.
- 2026-06-22: **Added a full-scroll "What We Do" services section as the 4th section on `/home-3`** (`Home3Services`), directly under the video-scroll section. It's a **cinematic scroll reveal**: a tall scroll region **pins a screen-height frame**, and as you scroll the headline **"What · We Do"** rises in, a **sphere blooms outward from the centre of the words**, a **dashed ring** fades and un-blurs behind the text, and **three service cards** slide up at the corners — **Reels & short-form**, **Brand & food photography**, and **Social media, handled** (rewritten in Sandspire's voice, not the interior-design copy from the reference). On a **deep near-black** background with an **orange** glow + orange card numbers, matching the site's dark sections. Rebuilt from a GSAP/Webflow reference using **`motion/react`** (`useScroll` + `useSpring` for the smooth "scrub" feel + `useTransform`) so **no new animation library was added**. New images: `public/reels/sphere.png` (the centre sphere) and `public/reels/ring.png` (the dashed ring, **CSS-inverted** so its dark strokes show on the dark background). On **phones / reduced-motion** it degrades to a calm stacked layout with everything visible — no pinned scrolling. **To change the cards:** edit the `SERVICES` array at the top of `Home3Services.tsx`. Verified: clean TypeScript (no type errors on the component).
- 2026-06-22: **Swapped the `/home-3` "On the feed for" marquee from text names to real brand logos.** The dark scrolling trust strip under the video-scroll section now shows each venue's **white logo** (from `public/reels/logos/`) instead of the brand name typed out — 3 Fils, Brix Journey, Brix Café (`brixtable.png`), Slrp Ramen, Bordo Mavi — each followed by the small orange dot separator. Logos render at a uniform 28px height (`h-7 w-auto`, capped at 140px wide) at 80% opacity so they sit evenly on the near-black background; the brand name is kept as the image `alt` text for accessibility. To change a logo, replace the PNG at `public/reels/logos/<brand>.png` or edit the `TRUST` array (now `{name, logo}` objects) at the top of `app/home-3/page.tsx`.
- 2026-06-22: **Added a cinematic "video scroll" section on `/home-3`, directly under the statement section** (`Home3VideoScroll`). A full-screen client reel sits **stuck to the bottom of the screen** while three short "scenes" (headings + a line each) **scroll up and over it** — left column: "A month of reels in a single shoot day" and "Photos that sell the room before anyone arrives"; right column (starts lower): "The whole feed, quietly handled". A **play button** floats centred over the video; clicking it opens a **pop-up player** of the same reel with sound (close with the ✕, the Escape key, or by clicking the dark background). Uses the **konbini** reel + poster from `public/reels/` — the same "360° Showreel" clip shown on `/home-2`. Self-contained — no extra libraries were installed (the play icon is an inline SVG, the pop-up is a small built-in modal). **To change the reel:** edit `VIDEO_SRC`/`VIDEO_POSTER` at the top of `Home3VideoScroll.tsx`; **to change the scene copy:** edit the `LEFT_SCENES` / `RIGHT_SCENES` arrays in the same file. Verified: clean TypeScript, no type errors on the page or component.
- 2026-06-22: **Added a kinetic "statement" section directly under the `/home-3` hero** (`Home3Statement`), rebuilt from a reference video. A big centered Geist sentence — "We turn one shoot day [reel] into a month of reels [reel] that fill real tables, not [reel] vanity charts." — **reveals word-by-word**, each word fading from a light cream-grey to near-black on a ~0.07s stagger, while **three rounded reel thumbnails pop in** (spring overshoot) at the gaps. It plays **once when scrolled into view** (so it auto-plays on load under the hero) and reuses real client reel posters from `public/reels/posters/` (3 Fils, Slrp, Brix Journey) as the inline chips. Edit the `TOKENS` array in `Home3Statement.tsx` to change the copy or pictures. Built with `motion/react`, honours reduced-motion (renders the finished sentence, no animation). Verified: `/home-3` returns 200, all words + three image chips present in server-rendered HTML, clean TypeScript.
- 2026-06-22: **Rebuilt the `/home-3` hero into an interactive "Instagram-story" reel carousel** (inspired by Elevate Social Studio, adapted for Sandspire). The static copy + 3-card cluster is gone; the hero is now a near-black, full-height stage with a **drag-to-scroll carousel of real client reels** that **fans out on load** and an **Instagram-story overlay** (progress bar, avatar, @handle, ⋯/✕) that **jumps to whichever reel you hover** — only the focused reel autoplays (the rest hold a poster frame), so seven videos never thrash. A **custom drag cursor** appears over the carousel on desktop. The bottom of the hero anchors the headline **"Make them hungry."** (left) and a one-line description (right), stacking centered on phones. New component **`ReelStoryCard`** (`components/sandspire/home3/ReelStoryCard.tsx`); the hero (`Home3Hero.tsx`) was rewritten to own the carousel, intro animation, playback-gating (pauses when off-screen), reduced-motion handling, and constraint measuring. **Seven reels live in `public/reels/`** (`3fils`, `bordomavi`, `brixcafe`, `brixjourney`, `kanji`, `konbini`, `slrp`) — muted/looping ~540px-wide MP4s (~9 MB total) with matching poster JPGs in `public/reels/posters/`. **To change the feed:** drop a new file at the same path (e.g. `public/reels/3fils.mp4` + `public/reels/posters/3fils.jpg`) or edit the `STORIES` list at the top of `Home3Hero.tsx`. The old `SandSpireCanvas` particle field is no longer used by the hero (file kept for reuse). Verified: page returns 200, hero markup + reel assets serve, clean TypeScript.
  - **Follow-up (same day): real brand logos in the story avatar.** The story overlay's circular avatar now shows each brand's **white logo** (from The Lab Holding's brand kit) instead of a poster crop. Logos live in `public/reels/logos/<key>.png` (rasterized small from the SVG lockups — ~5–15 KB each, white on a dark circle). The Brix card was aligned to the available logo: **"Brix Café" → "Brix Table"** (`@brixtable`), and its reel/poster were renamed `brixcafe.* → brixtable.*` to match. To change a logo, replace the PNG at the same path or edit the `logo` field in the `STORIES` array in `Home3Hero.tsx`.
  - **Follow-up (same day): the feed now drifts on its own.** After the cards deal in (~1.1s), the carousel becomes a **slow, seamless left-drifting marquee** (~22 px/s) — the reels are duplicated into a loop so it never hits an end. It **pauses while you hover or drag** and when the hero is off-screen, and the **story overlay rides whichever reel is nearest screen-centre** as it moves (hovering locks it to that reel). Reduced-motion users get a still, drag-only feed. Speed lives in the `DRIFT` constant in `Home3Hero.tsx`.
- 2026-06-22: **Added `/home-3` — a cinematic, conversion-focused landing page** built from the locked `/ss-brand-strategy`. New route `app/home-3/` (page + Lenis layout) and a small component set in `components/sandspire/home3/`: **`SandSpireCanvas`** (a sand-to-spire 2D-canvas particle field — no WebGL, scales particle count with width, pauses off-screen, draws a static frame under reduced motion), **`ReelCard`** (a 9:16 "reel" with a panning brand-gradient surface, play affordance, and an honest placeholder metric pill), **`Home3Hero`** (hero copy + CTAs + floating reel cluster), and **`Home3Faq`** (accordion). The page reuses the shared **`SiteNavBar`** and **`ScrollReveal`**. Sections: cinematic hero → brand marquee → problem → difference → services → process → reel proof-wall → pricing → "Make them hungry." → final CTA + FAQ → footer. All copy follows **BRAND-VOICE.md**; proof view counts are clearly-labeled placeholders (no fabricated numbers). Verified in-browser at **1440px, 390px** (no horizontal overflow, zero console errors); reduced-motion handled in CSS + canvas. Not linked from the public nav.
- 2026-06-22: **Consolidated to two fonts — Geist (headlines) + Plus Jakarta Sans (body).** Removed **Playfair Display** and **Colitez Serif** entirely from `app/layout.tsx`. Repointed `--font-display` to Geist (`--font-sans`) and dropped the italic styling on headings (`app/globals.css`). Removed the CSS overrides that forced headings to Plus Jakarta (`.home-web`, `.site-jakarta`, `#contact`/`#footer` heading rules, `.work-project-grid h2`, etc.) and flipped the inline heading classes in components (Hero, ServicesBento, CaseStudies, ContactFAQ, WhoIsSandspire, WorkProjectTemplate, WorkProjectGrid, About, Coming-soon) from `--font-body` to `--font-display`, so **all headings now render in Geist** while body copy and form controls stay Plus Jakarta Sans. Updated `/ss-brand-strategy` typography section to match. Verified `/`, `/home-2`, `/about`, `/contact`, `/work`, `/ss-brand-strategy` all return 200.
- 2026-06-22: **`/ss-brand-strategy` — added “Part III · Locked Brand Definition.”** The final, locked brand reference: brand name (Sandspire, high confidence), the positioning statement, primary (functional) + secondary (emotional) taglines, the community line (“Make them hungry.”), brand voice rules (use / never-use / mechanics / text-message + website-vs-social examples), the six-colour palette with hex swatches + accessibility notes, the typography hierarchy table, the 12-week launch theme map, and a brand-lock diagnosis. Third table-of-contents group added; header chip now reads “STRATEGY · RESEARCH · BRAND LOCK.” Still internal/`noindex`. The page now holds all three phases (strategy → research → brand lock).
- 2026-06-22: **`/ss-brand-strategy` — added “Part II · Market Research.”** Appended a cited market-research appendix below the strategy: validation that reels-first for restaurants is a sound bet (HubSpot, Morning Consult), the single unsolved problem, evidence cards with source links, a customer-pain table, the competitor landscape (3 saturated angles + a comparison table of UAE F&B agencies + the “name + reel + number” white space), UAE pricing benchmarks, a keep/drop/add proof table, the sharpened v2.1 positioning, page-by-page messaging implications, a research diagnosis, and an honest “gaps to close” callout. Second table-of-contents group + outbound source links added. Still internal/`noindex`.
- 2026-06-22: **Added `/ss-brand-strategy`** — an internal, `noindex` review page presenting Sandspire's consolidated **social-first brand strategy** (social-media content studio leading with reels + photos; web + AI secondary; UAE hospitality niche; five proof brands). Editorial cream/orange layout with sticky contents, positioning statement, service hierarchy, homepage direction, fixes, and an asset/info checklist. Static (no CMS dependency); not in the public nav.
- 2026-06-19: **Home-2 services bento** — the orange circle glow behind the analytics video tile is **25% smaller** on `/home-2`.
- 2026-06-19: **Mobile + typography polish** — “Tell us what you're building” contact section is easier to read on phones (tighter spacing, stacked layout). **About** is now all black with a studio photo and no small eyebrow text. **Contact**, **Work**, and **work project** pages use **Plus Jakarta Sans** consistently; project card titles on `/work` are smaller; Challenge/Solution blocks on project pages use Jakarta. **Contact form + footer** match across all pages. **Sandspire logo** loads from Sanity CDN again (fixed broken `/public` path).
- 2026-06-19: **All site media now lives on Sanity CDN** — photos, videos, logos, bento images, and work-project galleries were uploaded to Sanity; the **`public/` folder is empty** (down from ~22 MB). Run **`npm run verify:sanity`** to confirm CDN URLs, or **`npm run seed:sanity-site-content`** + **`npm run seed:sanity-work-projects`** to re-upload from a backup. The site logo, service diagrams, Brand Strategy cascade, and work-scroll videos/icons all load from Sanity.
- 2026-06-19: **Home-2 Our Work (mobile spacing)** — tightened the gap above the stacking videos and added a little more top padding to the sticky project description.
- 2026-06-19: **Home-2 Our Work on mobile** — project title, tags, and description stay **pinned at the top** while the videos stack below; nothing shows under the phone cards anymore.
- 2026-06-19: **Home-2 Our Work scroll videos now stack on mobile** the same way they do on desktop — cards stick and pile up as you scroll.
- 2026-06-16: **Home-2 work videos now fit the mobile viewport with side padding**, and each video gets a mobile-only caption below it.
- 2026-06-16: **Home-2 hero headline and FAQ heading now use the italic serif display font again**, matching the rest of the serif treatment on that page.
- 2026-06-16: **The web-design marquee is now about 2.5x larger on mobile only**, while the desktop size stays the same.
- 2026-06-16: **Removed the 8px vertical padding from the web-design cascade rows**, so the marquee sits tighter inside the services card.
- 2026-06-16: **Web-design cascade images now stretch instead of crop**, so the marquee tiles in the services card use fill-style fitting for every image in that component.
- 2026-06-16: **Pushed the web-design marquee a bit farther down again** so it sits lower inside the services card while still staying unclipped.
- 2026-06-16: **Reduced the web-design marquee top offset to 50px** so it still sits lower in the services card but not as deep as before.
- 2026-06-16: **Fixed the clipped web-design marquee by reserving real top space inside the services card**, and slightly overfilled the tiles so the images sit flush against their frames instead of showing edge padding.
- 2026-06-16: **Moved the web-design marquee inside the main services card 80px lower** so the website grid sits much deeper in the frame.
- 2026-06-16: **Moved the web-design marquee inside the main services card 20px lower** so the website grid sits a little deeper in the frame.
- 2026-06-16: **Web-design marquee inside the services card is now much smaller** on the main homepage, with the default cascade scale reduced to roughly one-fifth to one-half of the old size so the grid reads more like the Figma reference.
- 2026-06-16: **Web & AI homepage services bento now splits into two desktop columns at `lg`**, so the **Social Media Marketing** card sits on the right side on normal desktop widths instead of dropping below the left stack.
- 2026-06-16: **Work listing card images now prefer the built-in local project files** for the code-backed slugs, so the `/work` grid stays on the exact `brixcafe_img.png`, `brixjourney_img.png`, `kanji_img.png`, and `konbini_img.png` assets even when Sanity has only partial image data.
- 2026-06-16: **Web & AI homepage (`/`) restyle — only this page changed, not `/home-2`.** Dropped the **italic serif** font everywhere on this page; **every title** (all headings plus the footer tagline) now uses **Plus Jakarta Sans** (added a `.home-web` wrapper rule in `app/globals.css` that covers headings and any serif-styled element). The **"Who is Sandspire"** and **"What we do"** sections now sit on a **brand-black** background, and **"Recent work we're proud of"** is a **white panel with rounded top corners**. The **Web Design** card's website previews now **fill the whole card** (full-bleed cascade with the title overlaid), so there are no empty gaps. Verified on desktop and mobile.
- 2026-06-16: **Fixed blank-on-load on `/about` and `/contact` (mobile).** `ScrollReveal` used to render every block hidden (`opacity: 0`) until JavaScript revealed it, so above-the-fold content flashed blank until the page finished loading. It now renders content **visible by default** and only hides blocks that start **below the fold**, fading those in on scroll — so the top of every page shows instantly while scroll animations still work lower down.
- 2026-06-16: **Copy went live + images moved into Sanity.** The new copy wasn't showing because the live site reads **published Sanity documents** first (they were overriding the code). Re-ran the seeders so all new copy is now published in Sanity (`npm run seed:sanity-site-content` and `npm run seed:sanity-work-projects`). **Fixed missing main-homepage images:** the "Web Design" card pointed at deleted `Frame*.svg` files; it now uses the website screenshots in **`public/images/bento-web/`**, and these are **editable in Sanity** (Homepage v1 → "Web Design card — preview images"). **Uploaded images into Sanity** (served from `cdn.sanity.io`, with `/public` paths kept as automatic fallback): all **work-project images** (hero, gallery, result, client logo), the **Web Design bento screenshots**, the **client logos** in the marquee, and the existing **home-2 hero + bento photos**. To re-upload after changing files, set `SANITY_API_WRITE_TOKEN` and run the two seed commands again. **Still in code (not yet in Sanity):** decorative bento SVG frames, the AI service icon, and video poster images.
- 2026-06-16: **Brand voice + full copy rewrite.** Added **`BRAND-VOICE.md`** (tone-of-voice guide) and rewrote all site copy in the new voice. **Main homepage** (`/`) now leads with **web design + AI** (hero, service columns, "Who is Sandspire", services); **second homepage** (`/home-2`) now leads with **social media + content** (hero, Service Suite cards, FAQ). Also refreshed shared site description, footer tagline/blurb, contact headline/intro, both FAQ sets, the About page, the Work listing intro, every work-project challenge/solution/description, and the Coming-soon page. Header/footer button is now **"Start a project."** Most copy lives in **`lib/siteContentDefaults.ts`** and **`lib/workProjectDefaults.ts`** (override in Sanity to change it live).
- 2026-06-11: **`/home-2` Service Suite (mobile)** — Slightly larger text on the compact flip cards (titles and descriptions).
- 2026-06-11: **`/home-2` Service Suite (mobile)** — Compact **3-column** bento (Figma iPhone layout) only below **554px** wide; **554px and wider** uses the full desktop bento grid. Mobile grid spans the full browser width with smaller ~120px cards.
- 2026-06-11: **`/home-2` Service Suite** — The service icon diagram (top-left bento card) now has **animated connecting lines**: orange dashes flow along the paths between nodes, with small dots traveling the routes (like React Flow’s animating edges). Respects **reduced motion** settings.
- 2026-06-11: **`/home-2` Service Suite** — Brand Strategy cascade tiles size to each image’s aspect ratio, with **10px** padding, **rounded corners**, and **gray translucent borders**; seven **webp** files in `public/images/bento/`. Service icon card has **5px** side padding.
- 2026-06-11: **`/home-2` images** — Hero and bento photos prefer **Sanity uploads** when set; otherwise the site uses local fallbacks in `public/images/` so the page never shows blank. Bento photos **fill their card frames** edge-to-edge.
- 2026-06-11: **`/home-2` polish (14 layout fixes):** Service Suite **Brand Strategy** cascade is smaller, centered, and responsive; bento images align **top**; dot-pattern background removed from the AI card; **Service Suite** title uses **Colitez Serif** (bento card copy stays **Jakarta**). **Our Work** title is no longer sticky; orange glow aligns with the section top and reads **darker**; **3 Fils** copy shows immediately before scrolling; card labels and tags use **Jakarta**. Hero **h1** uses **Jakarta**. Contact form: removed the gray info box and orange button glow on home-2. **Footer** on home-2: **orange** background, **black** text, logo **blend mode**.
- 2026-06-11: **`/home-2` follow-up:** Footer uses the same **orange stripe glow** as Our Work (brighter). Work sidebar copy stays **pinned on the left** while cards stack (opacity crossfade only). Brand Strategy cascade and service icon are **larger**.
- 2026-06-11: **`/work` page:** Project card titles (e.g. Bordo Mavi) use **Plus Jakarta Sans** instead of the serif display font.
- 2026-06-11: **`/home-2` Our Work:** Left project copy is **vertically centered** next to the stacking phone cards on desktop.
- 2026-06-11: **`/home-2` nav:** Top bar uses the **same links as Homepage 1** (Services, Work, About, Contact).
- 2026-06-11: **`/home-2` Service Suite:** Bento grid width aligned with other sections so left/right padding matches.
- 2026-06-11: **`/home-2` Service Suite:** Entire bento grid locked to **Plus Jakarta Sans** (section title, card titles, flip text).
- 2026-06-11: **`/home-2` hero:** Softer bottom corner radius (**28px** mobile, **40px** desktop).
- 2026-06-11: **`/home-2`:** Removed sticky-scroll hero overlay; hero stays **full screen** with bottom marquee. Flip bento cards and Plus Jakarta Sans on hero/contact/footer remain.
- 2026-06-11: **Hero, contact form, and footer** use **Plus Jakarta Sans**; Colitez Serif stays on other section headings.
- 2026-06-11: **`package.json` name** set to **`sandspire`** so it matches the Cloudflare Worker in **`wrangler.jsonc`** (avoids “Worker not found” on deploy when OpenNext reads the package name).
- 2026-06-11: Added agent skill **`.claude/skills/cloudflare-sanity-deploy/`** — reusable checklist for Cloudflare Worker size limits, Sanity CMS performance (SSG/ISR, CDN images), mobile patterns, and pre-deploy audits (based on La Torta production learnings). Invoke with **`/cloudflare-sanity-deploy`** in Ship Studio.
- 2026-06-05: **`/home-2` nav** — Top bar stays **sticky** for the full page (not only inside the hero).
- 2026-06-05: **`/home-2` polish** — Centered hero copy; logo strip flush to hero bottom and full-width; larger hero image bleed; project text fully swaps (fade out before next fades in).
- 2026-06-05: **`/home-2` hero** — Swapped the hero video for **`public/images/HeroImage.png`** (unboxing photo).
- 2026-06-05: **Our Work on `/home-2`** — Each scroll step shows matching **left copy + right video** (3 Fils, Kanji, Slrp); all phone cards are the same size; orange glow bleeds to the right browser edge.
- 2026-05-14: **Added `/home-2`** from Figma [Things, node 349:142](https://www.figma.com/design/DAnc9vXaZN9VHQcCosnCm3/Things?node-id=349-142): video hero, featured work row, service suite grid, showreel, contact/FAQ with **`ContactFAQ` `variant="home2"`**, and **`app/home-2/layout.tsx`** for serif section headings.
- 2026-04-24: **Wrangler + OpenNext in CI:** **`wrangler.jsonc`** now includes a **`build.command`** (runs `scripts/wrangler-opennext-build.cjs` → `opennextjs-cloudflare build`) so **`npx wrangler deploy` only** in Cloudflare still produces **`.open-next/`** and avoids *Could not find compiled Open Next config*. Do not duplicate a dashboard **Build** + this hook without dropping one, or the app builds twice.
- 2026-04-24: **Cloudflare free-tier Worker size (~3 MB gzip):** **OpenNext** uses **`npx next build --webpack`** and a **webpack `NormalModuleReplacementPlugin`** so Vercel OG (`@vercel/og`, resvg/yoga) is not in the server bundle. **`@sentry/nextjs` `withSentryConfig` is disabled in production builds** (but not in **`next dev`**) unless **`OPENNEXT_WITH_SENTRY=1`** or **`npm run build:with-sentry`**, so the default deploy fits the 3 MB limit; **`@sentry/cloudflare` on `cf-worker-sentry.ts`** can still report errors if a DSN is set.
- 2026-04-24: **Sentry (Cloudflare Worker):** **`@sentry/cloudflare`** wraps the OpenNext-generated handler in **`cf-worker-sentry.ts`** while re-exporting OpenNext’s Durable Object classes. **`wrangler.jsonc` `main`** points to that file; **`nodejs_compat`** was already set. Set **`SENTRY_DSN`** or **`NEXT_PUBLIC_SENTRY_DSN`** on the Worker (same DSN as the Next project is fine). Optional source maps: run **`npx @sentry/wizard@latest -i sourcemaps --saas --org <org> --project <project>`** as in the Sentry docs. This is separate from but complements **`@sentry/nextjs`**, which you may tune if events duplicate.
- 2026-04-24: **Sentry:** Integrated **`@sentry/nextjs`** (errors, tracing, session replay, logs) per the official Sentry for AI Next.js skill: **`instrumentation.ts`**, **`instrumentation-client.ts`**, server/edge configs, **`app/global-error.tsx`**, and **`withSentryConfig()`** in **`next.config.ts`**. The SDK only initializes when **`NEXT_PUBLIC_SENTRY_DSN`** is set; set **`SENTRY_ORG`**, **`SENTRY_PROJECT`**, and **`SENTRY_AUTH_TOKEN`** in CI for source map upload. The tunnel runs at **`/monitoring`**. You can also run **`npx @sentry/wizard@latest -i nextjs`** for an interactive full setup. See **`.env.example`**.
- 2026-04-24: **Cloudflare deploy:** Documented and scripted **`cf:workers`** (same as **`npm run deploy`**) for Workers Builds: **`npx wrangler deploy` alone fails** with “Could not find compiled Open Next config” if **`npm run build` never runs**; use a build step, or a single command **`npm run deploy`**. **README** and **`wrangler.jsonc` comments** describe the two patterns.
- 2026-04-24: **Sanity seed + `.env`:** The work-project seed script now loads **`.env`** and **`.env.local`** and uses the same project/dataset/API defaults as **`sanity/env.ts`**, so **`SANITY_API_WRITE_TOKEN`** alone is enough for **`npm run seed:sanity-work-projects`** when you point at the default project.
- 2026-04-23: **Contact form:** **WhatsApp number** (required, text label only). **Message is optional** (up to 2,000 characters). **Client + API validation**; **`noValidate`** so fields aren’t red until **after a failed submit**; then invalid fields get a **rose border** and short inline errors. **POST `/api/contact`**; optional Resend / webhook in `.env`.
- 2026-04-23: **`/work` filters:** Pills are **case-insensitive** vs project tags, sync with the URL as **`/work?category=…`**, and the bar scrolls horizontally on small screens. Empty filters show a short message. **Coming soon** page at **`/coming-soon`**; contact social icons and footer **Social** list link there until profiles launch (includes **X/Twitter** icon in the contact block).
- 2026-04-23: **Home services bento (mobile + polish):** Removed the **orange accent lines** under card titles, added spacing between **Web Design** text and image, a bit more **section top padding** on small screens, and the **Social** video is **shorter and wider** on mobile. **Homepage** cream band and **#work** use **tighter top rounding** on mobile than desktop.
- 2026-04-23: **Mobile menu polish:** The menu toggle is **after “Get in touch”**, uses **stroke line icons** (round caps) instead of solid blocks, and the sheet is **inset** with **large rounded corners**, extra **padding**, and a **Motion** (open/close) animation plus dimmed **backdrop** blur. Respects **reduced motion**.
- 2026-04-23: **Homepage header:** The hero uses the same shared **`SiteNavBar`** as inner pages, so the **hamburger and mobile link sheet** work on the homepage (earlier, only `SandspireHeader` pages had the menu; the old hero had no menu button). The mobile sheet is **`position: fixed`** below the bar so the hero’s **`overflow-hidden`** does not clip it.
- 2026-04-23: **Contact block:** A **`tel:`** link for **+971 56 198 0747** appears in the contact intro column (including the full **Contact** page and the homepage contact section).
- 2026-04-23: **Mobile + homepage work strip:** The top bar shows a **hamburger menu** on small screens with the same links as desktop (**Services, Work, About, Contact**). The footer **Menu** list matches that set (plus **Home**) and uses the same routes, including **Work** → `/work` instead of a separate “Projects” hash link. On the homepage **#work** section, each project row (image + copy + CTA) is a single link to **`/work/{slug}`**; the **Brix Journey** logo strip uses that name in `alt` text. **Shared nav data** for header and footer lives in `components/sandspire/sandspireNav.ts` (re-exported from the header for anything that still imports from there).
- 2026-04-23: **Naming:** The Sanity document type and code are now **work project** (not “case study”), to match the **Work** section; **case study** is reserved for a possible future page type. Schema type **`workProject`**, file **`lib/workProjectDefaults.ts`**, **`WorkProjectTemplate`**, and **`npm run seed:sanity-work-projects`**. GROQ still accepts legacy **`caseStudy`** documents until you migrate them in the CMS.
- 2026-04-23: **Work listing + Sanity:** The **`/work`** page now loads its project cards from **work project** documents in Sanity (order, show/hide on the grid, listing title, short description, card image). Adding or removing a project in Sanity updates the grid and **`/work/{slug}`** (new slugs no longer require a matching line in the code defaults). The category filter is interactive. If the Sanity query returns nothing, the site uses the previous hardcoded list as a fallback.
- 2026-04-23: **Work / Sanity:** The **wordmark** on each project page now uses the work project document’s *Internal title* from Sanity when present (it previously always used the code default only).
- 2026-04-23: **Cloudflare Worker size:** The huge server bundle (Sanity’s embedded **Studio** on `/studio`) was removed so the site can be deployed; the public **`/studio`** page only explains how to open **Sanity Manage** or run **`npx sanity dev`**. The production Worker is still a few **megabytes** — it can hit **free-plan** Cloudflare size limits, so a **paid Workers** plan (or a different host) is often needed. Dependencies were also trimmed (for example `next-sanity` replaced with a smaller client) where it helped.
- 2026-04-23: **Deploy installs:** Project dependencies were adjusted so automated installs (for example on Cloudflare) pull **newer supporting libraries** behind the scenes; this removes noisy “deprecated package” messages during `npm install` without changing what visitors see on the site.
- 2026-04-23: **Polish & mobile case studies:** Sitewide **hover / focus** micro-interactions (nav underlines, CTA lift + shadow, footer link nudge, work cards, homepage case rows, contact form + submit, FAQ pills, gallery image scale on case studies). **Case study hero:** on viewports below `lg`, **copy/meta appears first** and the **device image below** (`flex-col-reverse` + `lg:flex-row`).
- 2026-04-23: **Work listing (`/work`):** Removed **`ScrollReveal`** on this page (no load/scroll-in animation). Each project card is a single Next.js link to **`/work/{slug}`** so the entire card opens the case study; “View Project” is plain text inside that link.
- 2026-04-23: **Sanity work fetch:** GROQ loads are **capped at ~12 seconds** per slug; on timeout or network error the page uses **`lib/workProjectDefaults.ts`** and logs a dev warning. **`getWorkProjectBySlug`** is **request-cached** so `generateMetadata` and the page don’t double-fetch.
- 2026-04-23: **`ScrollReveal` / back navigation:** Reveal blocks now drive visibility with **`useInView` + `animate`**, a layout-time geometry check (double **`requestAnimationFrame`** for scroll restoration), **`popstate`** remeasure, and **`pageshow`** when **`persisted`** (bfcache). Fixes `/work` (and other pages) looking blank after the browser **Back** button because content stayed at **`opacity: 0`**.
- 2026-04-23: **Case study layout:** The former “brand detail” image (**`resultImageTall`** / tall result asset) now sits **under the phone** in the cream gallery column on every **`/work/[slug]`** page. The **“The result”** heading and the **wide “case study cover”** image are removed from the template (Sanity fields for those may remain but are unused on the site).
- 2026-04-23: **Case study images:** Hero and cream-gallery images size to the file’s aspect ratio (**`h-auto w-full`**) inside rounded frames so there’s no extra empty band above or below the art; frame backgrounds stay **`#1f1f1f`** (hero) and **`#faf3e8`** (gallery).
- 2026-04-23: **Work projects:** Eight routes **`/work/{slug}`** share the work project template with defaults in **`lib/workProjectDefaults.ts`**; the seed script upserts **work project** documents. Work listing **View Project** links go to each slug ( **`/work/slrp`** unchanged).
- 2026-04-23: **Dev:** If Turbopack crashes with missing cache files under `.next`, delete **`.next`** and run **`npm run dev`** again; **`npm run dev:webpack`** uses webpack instead of Turbopack as a fallback.
- 2026-04-23: **Sanity work project:** Document type **`workProject`**; pages load from Sanity with code fallbacks; GROQ in **`sanity/lib/queries/workProject.ts`**; ISR **`revalidate = 60`**.
- 2026-04-23: **Sanity:** Documented linking the repo to the Sanity cloud project; added **`.env.example`** (with `!.env.example` in gitignore so it can be committed), and a starter **`homepage`** schema. Full **Studio** is run with **`npx sanity dev`** (or Sanity Manage on the web); the public site’s `/studio` route is a help page, not the full editor.
- 2026-04-23: **SLRP page polish:** Cream block uses the same outer width and horizontal padding as the rest of the site (`max-w-[1220px]`, `px-6` / `lg:px-10`) with the case-study column centered inside; removed the image-row drop shadow; hero inner container no longer has bottom padding; “Visit Website” points to `https://www.slrpramen.com/`.
- 2026-04-23: **SLRP page (`/work/slrp`):** Reworked to match Figma node **164-4853** — hero uses a subtle dot grid on charcoal, rounded tablet frame for `slrp_header`, inverted SLRP logo for contrast, Alexandria-style pills and metadata labels, external-link “Visit Website” control; middle gallery is `slrpBento3` over `slrpBento1` with a full-height `slrpBento2` beside them; “The result” is centered with a simple two-image row (`slrpBento4` + `slrpBento5`). Challenge/solution copy stays substantive single paragraphs.
- 2026-04-23: **Cloudflare build:** the standard production build now produces the OpenNext worker bundle (not only a plain Next.js build), so the step that runs after the build on Cloudflare can find the worker and static assets. This avoids failed or stuck deploys when only `next build` had run before.
- 2026-04-23: **Cloudflare deploy fix:** the Worker’s **self-reference** in Wrangler is set to the same name as the deployed script (`sandspire`) so production deploys no longer fail with “Worker not found” for the service binding.
- 2026-04-23: **Scroll experience:** in-page anchor jumps use smooth scrolling when the visitor has not asked for reduced motion. Major blocks **fade and lift into view** as you scroll (hero copy + logo strip, who-we-are columns, services bento, homepage work strip, contact vs FAQ, footer, about page sections) via a shared `ScrollReveal` component; the **`/work`** listing itself does not use it; motion is disabled when `prefers-reduced-motion` is on.
- 2026-04-23: **Hero readability:** the full-screen video and gradient sit on lower layers; headline and body copy sit above decorative edge overlays so text is no longer covered. **New pages:** **About** (`/about`) on the cream background and **Contact** (`/contact`) reusing the homepage contact + FAQ styling on a full dark page. **SandspireHeader** is shared on work, SLRP, about, and contact with links to Services, Work, About, and Contact; the homepage hero bar matches. Footer menu now includes **Contact**, **About**, and home/services/projects links that work from any page (`/#…` where needed). The old `/3d-marquee-demo` route is a short placeholder so builds don’t depend on a removed demo file.
- 2026-04-23: **Pricing** and **Use cases** are hidden from the top menu on the homepage, `/work`, and `/work/slrp`, and **Pricing** was removed from the footer menu until you are ready to show them. Typography is slightly smaller and more consistent (lighter nav and CTAs, tuned hero and section headings, bento card titles, case study copy, contact/FAQ, footer). The **Who is Sandspire** block (`#who`) has more space above and below; services, work, and contact sections have a bit more vertical padding. The dark **Contact** panel uses a single `70px` top radius plus `overflow-hidden` so the curved top edge paints cleanly.
- 2026-04-23: All headings inside the homepage `main` (work titles, contact titles, FAQ heading, services titles, who-we-are lead line, footer column labels) use light weight (300) for a consistent thin title style; CSS also sets `main h1–h6` to weight 300 as a baseline.
- 2026-04-23: Work / projects section (`#work`) now uses a solid white background and explicit dark text so it stays visible in dark mode (it had disappeared when system dark theme set the cream token to near-black while headings stayed dark). Contact + FAQ updated to match Figma (153-2017): rounded top on the dark panel, two-column contact layout, bordered glass form, field styling, pill FAQ grid, underlined “more questions” link. Web Design marquee uses stronger isometric tilt, tighter perspective, aggressive edge bleed, gap-6, taller tiles, max two tiles per row, and fills its card; services left column stretches; Social video vertically centered.
- 2026-04-23: Hero follows the Figma hero block layout (left kicker + large title, right two-line headline + supporting paragraph, bottom four service columns) with Sandspire-focused copy; service labels match the bento (#01–#04). Services: Social card gets a full-bleed brighter glass + orange/red radial glow like the Figma reference; bento headings use medium weight on titles and normal on price lines; AI dot pattern nudged further down-right; Web Design marquee bleeds past its frame and scales up to fill the card. “What we do” uses medium weight; Agency Services eyebrow is normal weight.
- 2026-04-23: Services bento is more compact overall (narrower max width, shorter section and card heights, smaller type, tighter gaps, 24px card rounding, smaller Web Design marquee tiles). The orange “results” video sits in a half-height clipped frame with stronger top rounding, pushed lower with extra bottom bleed. The AI Automation dot background is shifted further right. Social column video height is reduced to match the shorter layout.
- 2026-04-23: Services bento: doubled the AI Automation diagram size, scaled up both analytics videos (orange “results” card and tall Social card) with the Social clip pinned to the bottom of its frame, tightened the results card bottom padding by 10px and extended the media layer past the card bottom so the video crops cleanly at the edge, and raised the results card minimum height for the larger video.
- 2026-04-23: Web Design service card: marquee tiles are three times larger with a consistent 3px frame border, the left title column is narrower so the marquee has more width, and the card is taller so the bigger tiles fit; restored the optional skew wrapper on the marquee band for the Figma-style angle.
- 2026-04-23: Shifted the AI Automation card dot-pattern background slightly right and down and scaled it larger so the texture fills the card more naturally.
- 2026-04-23: Rebuilt the homepage “What we do” services bento to match the Figma frame (node 153-837): wider grid, orange rule lines above pricing, first card headline “See real results you can measure” with the analytics video on a dark rounded plate, AI Automation card with a local dot-matrix background behind the icon diagram, Web Design previews skewed to match the comp, and a taller glass-style Social column with warm gradient glows behind the phone mockup.
- 2026-04-20: Refined service-pill styling so outlines are now neutral glass and only the outer glow carries the category color.
- 2026-04-20: Softened the new glassmorphism service-pill glow intensity on work pages while keeping the same color mapping by service type.
- 2026-04-20: Restyled work/project category pills with a glass-outline look and subtle color glows by service type (orange for Branding/Social Media, yellow for Web Development, soft white for AI Automation).
- 2026-04-20: Added click-outside behavior to the FAQ so any open answer closes automatically when clicking anywhere outside the FAQ area.
- 2026-04-20: Fixed FAQ grid card behavior so opening one answer no longer visually stretches the neighboring card in the same row.
- 2026-04-20: Increased spacing between each FAQ question and its expanded answer text across all FAQ cards for easier reading.
- 2026-04-20: Updated the homepage hero dark gradient overlay layer so it is sized with full width/height from the bottom-right anchor instead of using full inset positioning.
- 2026-04-20: Fixed FAQ title alignment by removing closed-state answer spacing and increased FAQ pill inner padding from 5px to 15px.
- 2026-04-20: Updated FAQ row pills to hug their content with 5px inner padding and tighter row spacing.
- 2026-04-17: Made the `/work/slrp` case-study section smaller again: reduced section vertical padding and container widths, tightened image block max widths/gaps, and lowered supporting/portrait image minimum heights for a more compact composition.
- 2026-04-17: Adjusted `/work/slrp` asymmetrical image block alignment for tablet/desktop feedback: shifted the grid left (`ml-0 mr-auto`), increased inter-column spacing (`48px→64px`), and added slight left inset on the portrait column.
- 2026-04-17: Scaled the `/work/slrp` image section down further per follow-up: reduced section/container widths and padding again, tightened the column gap slightly, and lowered image minimum heights (portrait `680→600`, supporting `280→240`).
- 2026-04-17: Refined `/work/slrp` image section spacing and scale: increased left/right column separation to a wider editorial gap (`56px` on desktop), reduced overall section/container width and padding, and scaled image heights down (portrait `780→680`, supporting `320→280`) while preserving alignment and aspect behavior.
- 2026-04-17: Changed `/work/slrp` portrait side borders to render outside the frame by replacing `border-x` with left/right hard box-shadows (`±128px`) so image width stays intact.
- 2026-04-17: Increased the `/work/slrp` portrait visual side-only black borders by another 4x, from `32px` to `128px`.
- 2026-04-17: Increased the `/work/slrp` portrait visual side-only black borders from `8px` to `32px` (4x thicker) while keeping top/bottom borderless.
- 2026-04-17: Updated the `/work/slrp` portrait visual frame with thick black left/right-only borders (`8px`) while keeping top and bottom edges borderless.
- 2026-04-17: Added consistent glass-style image framing across all `/work/slrp` visuals (translucent borders, subtle inner highlight, soft shadow, and slight backdrop blur) for a unified polished look.
- 2026-04-17: Fixed homepage image sources: Web Design bento now loads marquee assets from `public/images/bento/`, and the “Crafting legacy for teams that scale” case rows now use `3fils_img`, `brixjourney_img`, and `slrp_img` from `public/images/projects/...`.
- 2026-04-17: Updated `/work/slrp` result row so the left image now stretches to match the right image height (`h-full`) while preserving the current width split.
- 2026-04-17: Made the right image in the `/work/slrp` result row wider again by changing the column split from `0.38/0.62` to `0.3/0.7`.
- 2026-04-17: Increased the size of the two `/work/slrp` result images by expanding their row container max width from `560px` to `860px`.
- 2026-04-17: Fixed root hydration mismatch warning by adding `suppressHydrationWarning` to the `<html>` element in `app/layout.tsx`, preventing client-side attribute injection differences from triggering runtime overlay errors.
- 2026-04-17: Rebalanced the `/work/slrp` result image row to a wider right column (`0.38fr / 0.62fr`) so the right image takes more horizontal space while keeping the existing row height behavior and spacing.
- 2026-04-17: Increased the `/work/slrp` “SLRP result detail one” image height to match the left result image while keeping the same column width.
- 2026-04-17: Adjusted `/work/slrp` result image row sizing: reduced both result images to a smaller centered two-column block and added rounded corners to the “SLRP result detail one” image.
- 2026-04-17: Removed the `/work/slrp` result metric number/text block entirely and simplified the section to title + editorial paragraph + two equal-height result images (`slrpBento5` and `slrpBento4`).
- 2026-04-17: Updated `/work/slrp` visuals per latest QA: added left/right stroke on the portrait image, removed the metric card article wrappers in the result stats, and changed result row images to `slrpBento5` (hero) and `slrpBento4` (detail).
- 2026-04-17: Applied another `/work/slrp` image-role pass: swapped portrait/supporting placements in the asymmetrical grid, moved the portrait asset to result level 1, and removed rounded corners from the result image row wrappers.
- 2026-04-17: Applied `/work/slrp` polish updates: removed the top hero media drop shadow, switched the link glyph from a diagonal arrow to a short right arrow, removed rounding from the three result metric cards, and reassigned result/portrait image assets based on the latest content feedback.
- 2026-04-17: Updated `/work/slrp` content structure again per QA: swapped visual roles in the asymmetrical image grid (portrait now dominant, supporting image reduced), rebuilt “The result” into a two-column heading/text layout, removed the extra “result detail two” image, and kept the remaining result images side by side underneath.
- 2026-04-17: Redesigned `/work/slrp` case-study content into a cleaner editorial layout: light beige canvas, flat two-column challenge/solution typography, structured asymmetrical image grid, rebuilt centered results block, and removed the extra dark gallery section to reduce visual noise.
- 2026-04-17: Centered the top `/work/slrp` split project grid and removed negative side offsets so the block no longer leaves redundant right-side space.
- 2026-04-17: Updated `/work/slrp` hero intro per feedback: removed the standalone top title/description and external tag strip, then moved the project title and `Branding`/`Web Development` tags into the right project info panel.
- 2026-04-17: Tweaked `/work/slrp` top project frame again: made the media frame much smaller in the split layout, kept rounded top corners (including top-right), and increased the SLRP header image negative bottom margin for a tighter crop.
- 2026-04-17: Updated `/work/slrp` hero media to use `slrp_header.png` and increased the image negative bottom margin for a tighter flush fit in the top project card.
- 2026-04-17: Applied `/work/slrp` spacing cleanup from QA: removed the right intro info panel border, removed bottom padding in the first bordered section, removed bottom padding from the left intro media card, and added negative bottom margin to the main SLRP image for a flush edge.
- 2026-04-17: Corrected both `/work` wrapper closing tags together (header inner `</div>` and first content `</section>`) to fully resolve the repeated parse errors around lines 91 and 122.
- 2026-04-17: Fixed another JSX mismatch in `/work` by changing the closing tag after the category pills from `</div>` to `</section>`, resolving the line-122 parse error.
- 2026-04-17: Fixed a second JSX closing-tag mismatch in `/work` header markup (`</section>` corrected to `</div>`) to resolve another parse-time build error.
- 2026-04-17: Fixed a JSX structure bug on `/work` (mismatched closing tag in the intro section) that was causing a build parsing error.
- 2026-04-17: Restored the previous `/work` portfolio listing page and moved the long case-study layout to `/work/slrp`, then wired “View Project” links from the listing to open the detail page.
- 2026-04-17: Applied another `/work` cleanup pass from visual QA: made the top frame hug only the sticky top bar (removed the extra 519px header height), removed bottom padding under the first bordered section, removed the dark background fill from the right intro info card, and removed bottom corner rounding on the intro image/card.
- 2026-04-17: Applied `/work` feedback pass: removed the decorative top pattern image layer, made the top sticky nav span full viewport width, made the intro project split flush to the container edges, and removed bottom corner rounding on the two intro cards.
- 2026-04-17: Updated `/work` so image containers now hug the natural image dimensions (removed fixed card image heights), and rebuilt the top header block to match the 519px dark Figma frame style with a subtle rotated dot-pattern background treatment.
- 2026-04-17: Rebuilt the `/work` page to match the new Figma direction for SLRP (dark intro with project details and visit button, cream challenge/solution/result storytelling block, updated local project image paths, and supporting gallery tiles before contact/footer).
- 2026-04-16: Added Figma MCP server to `.mcp.json` so the agent can connect to Figma.
- 2026-04-16: Added local Figma desktop MCP endpoint (`figma-desktop`) to `.mcp.json`.
- 2026-04-16: Implemented the Sandspire homepage from Figma (node `336-1207`) including hero/video, logo marquee, services bento, case studies, contact+FAQ, and footer.
- 2026-04-16: Filled in FAQ accordion answers so the FAQ section isn’t blank.
- 2026-04-16: Updated global style tokens to match the branding reference site (Sandspire color roles, typography, and radius primitives).
- 2026-04-16: Refined hero section to better match Figma (top glass nav bar, local video + fallback image, right-side hero text block, and 4-column service row).
- 2026-04-16: Updated logo scroll to use local files from `public/logos` instead of localhost MCP asset URLs.
- 2026-04-16: Updated hero copy styling to pure white and added continuous animated logo scrolling in the logo strip.
- 2026-04-16: Expanded hero top bar to full browser width while keeping nav content centered.
- 2026-04-16: Moved the section below the hero down so the full logo scroll strip stays visible.
- 2026-04-16: Rounded the bottom edges of the hero video area to match the curved screenshot treatment.
- 2026-04-16: Fixed hero corner clipping by moving the video/overlay layers inside the rounded container.
- 2026-04-16: Matched the “Who is Sandspire” block to the Figma two-column layout (1026px container, equal columns, larger description type).
- 2026-04-16: Added hero corner highlight/shadow overlays so the rounded bottom edge reads clearly on top of the video.
- 2026-04-16: Refined hero video rounding using a dedicated overflow-hidden wrapper (`rounded-b-[24px]`) and added top spacing before the logo ticker so corners stay visible.
- 2026-04-16: Increased hero bottom rounding (`rounded-b-[48px]`) and shifted hero/ticker background treatment to black for a darker, higher-contrast look.
- 2026-04-16: Reverted the latest logo marquee background styling to the previous gradient strip treatment.
- 2026-04-16: Hardened hero video clipping with `isolate` + `translateZ(0)` on the rounded wrapper, added `overflow-hidden` on the parent, and mirrored `rounded-b-[24px]` on the video as fallback.
- 2026-04-16: Increased hero rounding again to `rounded-b-[48px]` for a stronger curved bottom edge.
- 2026-04-16: Made the hero top navigation bar semi-transparent for better video visibility.
- 2026-04-16: Made the hero top menu sticky and set each homepage section to fill at least one browser screen height.
- 2026-04-16: Vertically centered the “Who is Sandspire” section content and increased hero video area to 70% of viewport height.
- 2026-04-16: Implemented the “What we do” section using local project assets/videos and refined the bento card proportions to match the intended design.
- 2026-04-16: Rebuilt the “What we do” bento to match the Figma frame structure (1014px heading width, 650/337 split, 347/350/718 card heights, and 29px spacing) with local media assets.
- 2026-04-16: Replaced the services block with a pixel-tight dark bento layout using DM Sans, exact card titles/pricing, rust-to-maroon gradients, chart rings, Instagram phone mockup, and responsive CSS Grid behavior.
- 2026-04-16: Added a standalone `public/scattered-tablet-mockups.html` demo with a diagonal cascading tablet fan effect (7 framed mockups, perspective tilt, layered shadows, and staggered hover animation).
- 2026-04-16: Ported the scattered tablet fan effect into the live Brand Strategy card in `ServicesBento` with perspective tilt, color-framed mockups, overlap depth, and staggered hover lift animation.
- 2026-04-16: Integrated a shadcn-style `ThreeDMarquee` UI component (`components/ui/3d-marquee.tsx`) plus demo route (`/3d-marquee-demo`), and added dependencies (`motion`, `clsx`, `tailwind-merge`) with shared `cn` helper at `lib/utils.ts`.
- 2026-04-16: Applied `ThreeDMarquee` to the live Brand Strategy card in the services section, using local `public/images/Frame*` assets for the marquee tiles.
- 2026-04-16: Fixed Brand Strategy visibility by adding a compact mode to `ThreeDMarquee` and enabling it in the card so the marquee stays in-frame in shorter containers.
- 2026-04-16: Reworked compact `ThreeDMarquee` mode into an in-frame diagonal stack renderer (color-framed overlapping tiles) so Brand Strategy images are reliably visible.
- 2026-04-16: Restored marquee behavior in compact Brand Strategy mode with continuous alternating vertical motion per tile while keeping the stack in-frame.
- 2026-04-16: Updated compact Brand Strategy mode to a traditional continuous marquee loop (duplicated tile track sliding linearly) while preserving diagonal perspective and framed-card styling.
- 2026-04-16: Removed compact marquee tile borders and changed the motion to a diagonal continuous scroll with individually tilted image tiles.
- 2026-04-16: Matched the Brand Strategy reference layout by switching compact mode to 3 horizontal marquee rows with framed thumbnails, where each row scrolls back-and-forth independently.
- 2026-04-16: Updated `ThreeDMarquee` compact tiles to image-only rendering (no extra frame wrapper/padding/background/border), adjusted tile size to `128x72`, and switched `ThreeDMarqueeDemo` to local imported image assets with `compact` enabled.
- 2026-04-16: Applied the same image-only compact marquee treatment to the live Brand Strategy card by removing the extra marquee container ring/background wrapper in `ServicesBento`.
- 2026-04-16: Rebuilt the next section (`CaseStudies`) to match the Figma row layout: 1016px content width, large 2-column rows (598/383 split), 3 project entries, local case images, and dark rounded "Read More" CTA buttons.
- 2026-04-16: Simplified `CaseStudies` visuals so each left panel now shows only the local image filling the full card area (removed extra overlays/inset framing).
- 2026-04-16: Matched `CaseStudies` styling closer to the latest reference with centered headline, softer card radius/border shadow, vertically-centered right content, and smaller dark pill “Read More” buttons with orange icon badges.
- 2026-04-16: Restyled `CaseStudies` to the latest cream editorial reference: isolated cream section panel, lighter headline weight, tighter compact row spacing, layered dual-screen image composites, dark text hierarchy, and compact dark CTA pills.
- 2026-04-16: Made the projects section full-width across the viewport (while keeping inner content constrained), and removed full-browser-height behavior from the “Who is Sandspire” section.
- 2026-04-16: Set CaseStudies project visuals to a fixed 2:3 height:width proportion (`aspect-[3/2]`), and made overlay screen sizing relative so the layered composition scales with that ratio.
- 2026-04-16: Removed the extra overlapping frame image from each CaseStudies project visual so only the main local project image is shown.
- 2026-04-16: Rebuilt Contact + FAQ to match the dark Figma container: left contact copy + social circles, right glass-style form card with pill inputs, centered FAQ heading, and 2-column rounded dark question rows with expandable answers.
- 2026-04-16: Rebuilt the footer to match the latest Figma block (`1277x417` container, `1014x235` inner layout): dark background, left brand/tagline/description/copyright stack, and right Menu + Social columns with orange titles and muted link text.
- 2026-04-16: Made the whole homepage feel generally smaller by reducing typography scale, spacing, card heights, section paddings, button sizes, and logo strip sizing across hero, services, projects, contact/FAQ, and footer.
- 2026-04-16: Matched the “Who is Sandspire?” title styling to the “What we do” section title (same display font, lighter weight, and heading scale).
- 2026-04-16: Replaced the bento frame visual in the services section with `Service Icon Group.svg` from local assets.
- 2026-04-16: Reduced the size of the new `Service Icon Group.svg` visual inside the services bento card so it appears more compact.
- 2026-04-16: Reduced the `Service Icon Group.svg` visual further and centered it in the Web Design bento card.
- 2026-04-16: Moved the centered `Service Icon Group.svg` visual lower in the Web Design card for better vertical placement.
- 2026-04-16: Applied layout feedback pass: added top spacing to services bento cards, lowered media content inside info cards, added smooth edge fade masks on the logo marquee strip, top-aligned case-study text with lighter titles, and reduced FAQ row height.
- 2026-04-16: Applied service-card content feedback: removed the services subtitle line under “What we do”, moved that copy into the top-left of the first card, removed the first card title/price label, and updated the remaining card labels/prices to match the latest review notes.
- 2026-04-16: Refined services bento again per review: removed the extra absolute overlay label, renamed the first visible info card title to “AI Automation”, increased top padding inside all service cards, and changed service card titles to medium weight.
- 2026-04-16: Reduced service card title font weight again in the bento section for a lighter look.
- 2026-04-16: Added a new `/work` page inspired by the provided Figma frame (top nav, centered page intro, category bar, 2-column project showcase cards, and existing contact/footer sections), and linked the homepage top-bar “Work” item to `/work`.

## How to Customize
- To change the site name/tagline: edit `app/layout.tsx` (`metadata.title` / `metadata.description`) and/or `app/page.tsx`.
- To change heading fonts: the Colitez Serif file lives in **`app/fonts/`**; Playfair Display loads from Google as fallback. Both are wired in **`app/layout.tsx`**.
- To enable more MCP integrations: edit `.mcp.json`.

